# RV32I 五级流水线 CPU 技术文档

## 目录

1. [项目概述](#1-项目概述)
2. [五级流水线基础实现](#2-五级流水线基础实现)
3. [各级流水线详细功能](#3-各级流水线详细功能)
   - [3.1 IF 阶段（指令获取）](#31-if-阶段指令获取)
   - [3.2 ID 阶段（指令解码）](#32-id-阶段指令解码)
   - [3.3 EX 阶段（执行）](#33-ex-阶段执行)
   - [3.4 MEM 阶段（内存访问）](#34-mem-阶段内存访问)
   - [3.5 WB 阶段（写回）](#35-wb-阶段写回)
4. [级间通信和数据传输](#4-级间通信和数据传输)
5. [BTB分支预测器实现](#5-btb分支预测器实现)
6. [Forwarding实现](#6-forwarding实现)
7. [乘法器实现](#7-乘法器实现)
8. [除法器实现](#8-除法器实现)
9. [jal/jalr指令实现](#9-jaljalr指令实现)
10. [lb/lh/sb/sh指令实现](#10-lblhsbsh指令实现)

---

## 1. 项目概述

### 1.1 项目背景和目标

本项目是一个基于 RISC-V 架构的 RV32IM 五级流水线 CPU 实现，使用 Assassyn 硬件描述语言编写。RV32IM 指令集是 RISC-V 基础整数指令集（RV32I）加上乘法和除法扩展（RV32M）的组合，提供了完整的整数运算能力，包括算术、逻辑、移位、比较、分支、跳转、内存访问以及乘除法操作。

项目的核心目标是在保持代码简洁和可维护性的同时，实现一个高性能的五级流水线处理器。通过引入先进的优化技术，如动态分支预测和数据前递机制，显著提升了处理器的执行效率。此外，项目还实现了 Wallace Tree 乘法器和 Radix-4 SRT 除法器，这些专门的硬件加速器大幅提升了乘除法运算的性能。

### 1.2 技术栈

本项目的核心技术是 Assassyn 语言，这是一门基于 Python 的硬件描述语言（RTL），它通过装饰器和操作符重载提供了类似软件编程的硬件设计体验。Assassyn 语言的主要优势在于其简洁的语法和强大的抽象能力，使得开发者可以用更少的代码实现复杂的硬件逻辑。

项目中的硬件模块主要分为两类：Module 和 Downstream。Module 类用于构建时序逻辑模块，包含流水线寄存器和状态存储；Downstream 类用于构建组合逻辑模块，处理信号转换和条件判断。通过这两种模块的配合，项目实现了完整的五级流水线架构。

在硬件构建方面，项目使用了 RegArray 来实现寄存器文件和流水线寄存器，使用 SRAM 来实现数据内存。SRAM 模块提供了高效的内存访问接口，支持读写使能控制和地址译码。

### 1.3 指令集支持（RV32IM）

本 CPU 完整支持 RISC-V RV32IM 指令集，包括以下指令类别：

**RV32I 基础整数指令集：**

- **算术运算指令**：包括加法（ADD）、减法（SUB）等基本运算
- **逻辑运算指令**：包括与（AND）、或（OR）、异或（XOR）等位运算
- **移位指令**：包括逻辑左移（SLL）、逻辑右移（SRL）、算术右移（SRA）
- **比较指令**：包括小于（SLT）和无符号小于（SLTU）
- **立即数指令**：包括加立即数（ADDI）、与立即数（ANDI）、或立即数（ORI）、异或立即数（XORI）
- **加载指令**：包括加载字（LW）、加载半字（LH）、加载字节（LB）及其无符号版本（LHU、LBU）
- **存储指令**：包括存储字（SW）、存储半字（SH）、存储字节（SB）
- **分支指令**：包括等于跳转（BEQ）、不等于跳转（BNE）、小于跳转（BLT）、大于等于跳转（BGE）及其无符号版本（BLTU、BGEU）
- **跳转指令**：包括无条件跳转并链接（JAL）、寄存器跳转并链接（JALR）
- **立即数指令**：包括加载高位立即数（LUI）、添加高位立即数到 PC（AUIPC）

**RV32M 乘除法扩展指令集：**

- **乘法指令**：包括有符号乘法（MUL）、有符号乘法高位（MULH）、有符号乘无符号高位（MULHSU）、无符号乘法高位（MULHU）
- **除法指令**：包括有符号除法（DIV）、无符号除法（DIVU）
- **取余指令**：包括有符号取余（REM）、无符号取余（REMU）

### 1.4 主要特性

本 CPU 实现了多项先进的处理器设计技术，主要包括：

**五级流水线架构：**

CPU 采用经典的五级流水线设计，将指令执行过程分解为指令获取（IF）、指令解码（ID）、执行（EX）、内存访问（MEM）和写回（WB）五个阶段。这种设计允许多条指令在不同阶段并行执行，大幅提高了指令吞吐量。

**BTB 分支预测器：**

为了减少分支指令带来的流水线停顿，本 CPU 实现了分支目标缓冲区（Branch Target Buffer，BTB）预测器。BTB 包含 64 个表项，使用 PC 的低 6 位作为索引。每个 BTB 表项存储预测的目标地址和有效标志。配合 2 位饱和计数器（BHT），BTB 可以根据分支的历史行为动态调整预测策略，提高预测准确率。

**数据前递机制：**

为了解决数据冒险问题，本 CPU 实现了完整的数据前递机制。当检测到后续指令需要使用前序指令的执行结果时，系统会将结果直接从 MEM 阶段或 WB 阶段前递到 EX 阶段，避免了流水线暂停。前递逻辑支持从 MEM 阶段和 WB 阶段前递 ALU 结果，并且能够正确处理 Load 指令的数据前递。

**Wallace Tree 乘法器：**

乘法器采用 Wallace Tree 架构，通过多级 Carry-Save Adder（CSA）压缩技术，将 32 位乘法的延迟压缩到 3 个周期。Wallace Tree 的核心思想是通过并行压缩减少关键路径的延迟，相比传统的阵列乘法器具有更好的性能。乘法器支持符号转换法，可以正确处理有符号和无符号乘法。

**Radix-4 SRT 除法器：**

除法器采用 Radix-4 SRT（Sweeney-Robertson-Tocher）算法，每次迭代产生 2 位商，总共需要 16 次迭代产生 32 位商。整个除法过程需要 18 个周期完成，包括初始化、迭代和最终修正三个阶段。除法器实现了完整的状态机，支持有符号和无符号除法以及取余运算。

**冒险检测和处理：**

HazardUnit 模块负责检测和处理各种冒险情况，包括数据冒险、控制冒险和结构冒险。对于 Load-Use 冒险，系统会插入流水线气泡；对于乘除法指令，系统会暂停流水线等待指令完成；对于分支预测错误，系统会刷新流水线并从正确的地址重新开始执行。

**读-修改-写机制：**

对于字节存储（SB）和半字存储（SH）指令，MEM 阶段实现了读-修改-写（RMW）机制。由于 SRAM 只支持字级别的访问，SB 和 SH 指令需要先读取整个字，修改对应的字节或半字，然后写回整个字。这个过程通过状态机控制，确保原子性和正确性。

---

## 2. 五级流水线基础实现

### 2.1 流水线架构概述

本 CPU 采用经典的五级流水线架构，将指令的执行过程分解为五个独立的阶段，每个阶段负责特定的任务。这种设计允许多条指令在不同阶段并行执行，从而显著提高指令吞吐量和整体性能。

五级流水线的各个阶段依次为：指令获取（Instruction Fetch，IF）、指令解码（Instruction Decode，ID）、执行（Execute，EX）、内存访问（Memory Access，MEM）和写回（Write Back，WB）。每个阶段在一个时钟周期内完成其任务，然后将结果传递给下一阶段。

在理想情况下，五级流水线每个时钟周期可以完成一条指令，从而实现每个周期一条指令的吞吐量。然而，由于数据冒险、控制冒险和结构冒险的存在，实际性能会受到一定影响。本项目通过数据前递、动态分支预测和冒险检测等技术，最大限度地减少了这些冒险带来的性能损失。

### 2.2 各级流水线寄存器定义

流水线寄存器用于在相邻的流水线阶段之间传递数据和状态信息。每个流水线寄存器都是一个寄存器数组，在时钟边沿捕获前一阶段的输出，并在下一个周期提供给下一阶段。

**IF/ID 流水线寄存器：**

IF/ID 寄存器组位于指令获取阶段和指令解码阶段之间，包含以下寄存器：

- if_id_pc：存储当前指令的程序计数器值，32 位宽
- if_id_instruction：存储从指令内存中获取的指令，32 位宽
- if_id_valid：标志当前 IF/ID 寄存器中的指令是否有效，1 位宽
- if_id_prediction_info：存储 BTB 预测信息，34 位宽，包括 BTB 命中标志、预测跳转标志和预测的 PC 值

**ID/EX 流水线寄存器：**

ID/EX 寄存器组位于指令解码阶段和执行阶段之间，包含以下寄存器：

- id_ex_pc：存储当前指令的程序计数器值，32 位宽
- id_ex_control：存储解码阶段生成的控制信号，48 位宽，包括 ALU 操作码、内存读写控制、寄存器写使能等
- id_ex_valid：标志当前 ID/EX 寄存器中的指令是否有效，1 位宽
- id_ex_rs1_idx：存储源寄存器 1 的索引，5 位宽
- id_ex_rs2_idx：存储源寄存器 2 的索引，5 位宽
- id_ex_immediate：存储解码后的立即数，32 位宽
- id_ex_need_rs1：标志当前指令是否需要读取源寄存器 1，1 位宽
- id_ex_need_rs2：标志当前指令是否需要读取源寄存器 2，1 位宽
- id_ex_prediction_info：存储从 IF 阶段传递的预测信息，34 位宽

**EX/MEM 流水线寄存器：**

EX/MEM 寄存器组位于执行阶段和内存访问阶段之间，包含以下寄存器：

- ex_mem_pc：存储当前指令的程序计数器值，32 位宽
- ex_mem_control：存储控制信号，48 位宽
- ex_mem_valid：标志当前 EX/MEM 寄存器中的指令是否有效，1 位宽
- ex_mem_result：存储 ALU 执行结果，32 位宽
- ex_mem_data：存储要写入内存的数据，32 位宽
- ex_mem_pc_change：标志 PC 是否需要改变，1 位宽
- ex_mem_target_pc：存储目标 PC 值，32 位宽
- ex_mem_prediction_result：存储预测结果，103 位宽，包括预测错误标志、实际跳转标志等
- ex_sig_control：存储执行阶段输出的控制信号，48 位宽

**MEM/WB 流水线寄存器：**

MEM/WB 寄存器组位于内存访问阶段和写回阶段之间，包含以下寄存器：

- mem_wb_control：存储控制信号，48 位宽
- mem_wb_valid：标志当前 MEM/WB 寄存器中的指令是否有效，1 位宽
- mem_wb_mem_data：存储从内存读取的数据，32 位宽
- mem_wb_ex_result：存储执行阶段的 ALU 结果，32 位宽
- mem_wb_addr：存储内存访问地址，32 位宽

### 2.3 流水线控制机制

流水线的正常运行需要精确的控制机制来处理各种情况，包括正常指令流动、流水线暂停、流水线刷新和预测错误恢复。

**正常指令流动：**

在无冒险的情况下，每个时钟周期所有流水线寄存器都会更新，指令从 IF 阶段依次流向 WB 阶段。IF 阶段从指令内存获取新指令，ID 阶段解码指令并生成控制信号，EX 阶段执行 ALU 操作，MEM 阶段访问内存，WB 阶段将结果写回寄存器文件。

**流水线暂停：**

当检测到数据冒险时，流水线需要暂停以避免使用错误的数据。主要的暂停情况包括：

- Load-Use 冒险：当一条加载指令后紧跟一条需要使用加载结果的指令时，需要暂停一个周期
- 乘除法指令执行：乘法器需要 3 个周期完成，除法器需要 18 个周期完成，期间流水线需要暂停
- SB/SH 读-修改-写：字节和半字存储需要两个周期完成读-修改-写操作

暂停时，IF 阶段和 ID 阶段停止更新，EX、MEM、WB 阶段继续执行。暂停信号由 HazardUnit 生成，通过 stall 寄存器传递到 IF 阶段。

**流水线刷新：**

当检测到控制冒险（如分支预测错误或跳转指令）时，需要刷新流水线，清除正在 IF 和 ID 阶段的指令。刷新时，IF/ID 寄存器被清零，PC 被设置为正确的目标地址。刷新信号由 HazardUnit 根据 EX 阶段的预测验证结果生成。

**预测错误恢复：**

当 BTB 预测错误时，系统需要从正确的目标地址重新开始执行。恢复过程包括：刷新 IF/ID 寄存器，更新 PC 到正确的目标地址，并更新 BTB 和 BHT 以提高未来的预测准确率。

**SB/SH 状态机：**

为了处理字节存储和半字存储的读-修改-写操作，MEM 阶段实现了一个简单的状态机。状态机有三种状态：IDLE（空闲）、READ（读取）和 WRITE（写入）。在 IDLE 状态检测到 SB 或 SH 指令时，进入 WRITE 状态，在下一个周期完成写入操作后返回 IDLE 状态。

---

## 3. 各级流水线详细功能

### 3.1 IF 阶段（指令获取）

IF 阶段是流水线的第一个阶段，负责从指令内存中获取指令，并执行分支预测操作。该阶段的主要功能包括指令获取、PC 计算、BTB 查询和预测信息生成。

**指令获取：**

IF 阶段首先根据当前的 PC 值计算指令地址。由于指令是 32 位宽，而内存是按字节编址的，因此需要将 PC 值右移 2 位得到字地址。然后使用这个字地址从指令内存中读取指令。指令内存是一个寄存器数组，容量为 2048 条指令，足够存储大多数测试程序。

**PC 计算：**

在正常情况下，下一条指令的 PC 值是当前 PC 加 4。这是因为在 RISC-V 架构中，所有指令都是 4 字节对齐的。然而，当分支预测器预测跳转时，PC 值会被设置为预测的目标地址，而不是 PC 加 4。

**BTB 查询：**

BTB（Branch Target Buffer）查询是 IF 阶段的关键功能之一。BTB 是一个包含 64 个表项的缓存，用于存储分支指令的目标地址。BTB 使用 PC 的低 6 位（第 2 到 7 位）作为索引，因为分支指令通常是 4 字节对齐的，所以 PC 的最低两位总是 0。

BTB 查询过程包括以下步骤：

1. 从 PC 中提取索引值（第 2 到 7 位）
2. 使用索引读取 BTB 表项，得到预测的目标地址
3. 读取对应的 BHT（Branch History Table）表项，得到 2 位饱和计数器值
4. 读取对应的 BTB 有效标志

**预测信息生成：**

预测信息由三个部分组成：BTB 命中标志、预测跳转标志和预测的 PC 值。

BTB 命中标志直接从 BTB 有效标志中获取。如果 BTB 有效标志为 1，表示该 PC 地址对应的 BTB 表项有效，即之前执行过该分支指令。

预测跳转标志根据 BHT 值确定。BHT 是一个 2 位饱和计数器，其值范围从 0 到 3。当 BHT 值大于等于 2 时，预测分支会跳转；否则预测分支不跳转。这种 2 位饱和计数器机制可以平滑分支行为的变化，提高预测准确率。

预测的 PC 值根据 BTB 命中标志和预测跳转标志确定。如果 BTB 命中且预测跳转，预测的 PC 值为 BTB 中存储的目标地址；否则预测的 PC 值为 PC 加 4。

**IF/ID 寄存器更新：**

IF/ID 寄存器在时钟边沿更新，将当前周期获取的指令和相关信息传递给 ID 阶段。更新逻辑考虑了暂停信号的影响。当暂停信号为 1 时，IF/ID 寄存器被清零，相当于插入一个气泡；否则 IF/ID 寄存器被更新为当前获取的指令和相关信息。

**输出信号生成：**

IF 阶段的输出信号包括指令和预测信息。输出信号的生成逻辑考虑了 IF/ID 有效标志和暂停信号的影响。当 IF/ID 有效标志为 1 且暂停信号为 0 时，输出当前获取的指令；当 IF/ID 有效标志为 1 且暂停信号为 1 时，输出 0（相当于 NOP）；当 IF/ID 有效标志为 0 时，输出 IF/ID 寄存器中存储的指令。

### 3.2 ID 阶段（指令解码）

ID 阶段是流水线的第二个阶段，负责解析指令格式，提取操作码、功能码和立即数，生成控制信号，并将相关信息传递给 EX 阶段。该阶段还负责传递从 IF 阶段获取的预测信息。

**指令格式解析：**

ID 阶段首先解析指令的各个字段。RISC-V 指令集支持多种指令格式，包括 R 型、I 型、S 型、B 型、U 型和 J 型。不同格式的指令有不同的字段布局，但都包含操作码字段，用于确定指令的基本类型。

操作码字段位于指令的第 0 到 6 位（最低 7 位），用于确定指令的基本类型。例如，操作码为 0110011 表示 R 型指令，操作码为 0010011 表示 I 型指令。

除了操作码外，ID 阶段还解析以下字段：

- rd：目标寄存器索引，位于指令的第 7 到 11 位
- func3：功能码 3，位于指令的第 12 到 14 位
- rs1：源寄存器 1 索引，位于指令的第 15 到 19 位
- rs2：源寄存器 2 索引，位于指令的第 20 到 24 位
- funct7：功能码 7，位于指令的第 25 到 31 位

**立即数提取和符号扩展：**

不同类型的指令有不同的立即数格式和位置。ID 阶段根据指令类型提取相应的立即数，并进行符号扩展。

I 型立即数位于指令的第 20 到 31 位，共 12 位。符号扩展时，使用第 31 位作为符号位，高 20 位用符号位填充。

S 型立即数由两部分组成：第 25 到 31 位（高 7 位）和第 7 到 11 位（低 5 位），共 12 位。符号扩展时，使用第 31 位作为符号位，高 20 位用符号位填充。

B 型立即数由四部分组成：第 31 位（符号位）、第 7 位、第 25 到 30 位和第 8 到 11 位，共 13 位。符号扩展时，使用第 31 位作为符号位，高 19 位用符号位填充，最低位填充 0（因为分支目标地址是 2 字节对齐的）。

U 型立即数位于指令的第 12 到 31 位，共 20 位。U 型立即数不需要符号扩展，而是左移 12 位（低 12 位填充 0）。

J 型立即数由四部分组成：第 31 位（符号位）、第 12 到 19 位、第 20 位和第 21 到 30 位，共 21 位。符号扩展时，使用第 31 位作为符号位，高 11 位用符号位填充，最低位填充 0（因为跳转目标地址是 2 字节对齐的）。

**指令类型识别：**

ID 阶段根据操作码识别指令类型，包括 R 型、I 型、L 型（加载）、S 型（存储）、B 型（分支）、J 型（跳转）、JR 型（寄存器跳转）、LUI 型和 AUIPC 型。

对于 R 型指令，还需要检查 funct7 字段以区分不同的运算类型。特别地，当 funct7 为 0000001 时，表示这是 M 扩展指令（乘除法指令）。

**M 扩展指令解码：**

M 扩展指令包括乘法指令和除法指令。乘法指令根据 func3 字段区分：func3 为 000 表示 MUL（有符号乘法，返回低 32 位），001 表示 MULH（有符号乘法，返回高 32 位），010 表示 MULHSU（有符号乘无符号，返回高 32 位），011 表示 MULHU（无符号乘法，返回高 32 位）。

除法指令也根据 func3 字段区分：func3 为 100 表示 DIV（有符号除法），101 表示 DIVU（无符号除法），110 表示 REM（有符号取余），111 表示 REMU（无符号取余）。

**控制信号生成：**

ID 阶段生成 48 位的控制信号，用于控制 EX、MEM 和 WB 阶段的行为。控制信号包括以下部分：

- ALU 操作码（5 位）：指定 ALU 执行的操作类型，包括加法、减法、逻辑运算、移位和比较
- 内存读使能（1 位）：指示是否需要从内存读取数据
- 内存写使能（1 位）：指示是否需要向内存写入数据
- 寄存器写使能（1 位）：指示是否需要将结果写回寄存器文件
- 内存到寄存器（1 位）：指示写回数据是来自内存还是 ALU 结果
- ALU 输入选择（2 位）：指定 ALU 的第二个输入是来自寄存器、立即数还是 PC
- 分支操作码（3 位）：指定分支指令的类型
- 跳转指令标志（1 位）：指示是否为 JAL 指令
- 寄存器跳转标志（1 位）：指示是否为 JALR 指令
- 加载类型（3 位）：指定加载指令的类型（LB、LH、LW、LBU、LHU）
- 存储类型（2 位）：指定存储指令的类型（SB、SH、SW）
- 目标寄存器索引（5 位）：指定结果要写入的寄存器
- 立即数低 12 位（12 位）：存储立即数的低 12 位，用于后续阶段使用
- 乘法操作码（3 位）：指定乘法指令的类型
- 除法操作码（3 位）：指定除法指令的类型
- ALU A 零标志（1 位）：指示 ALU 的第一个输入是否应该为零（用于 LUI 指令）

**寄存器需求判断：**

ID 阶段还判断当前指令是否需要读取源寄存器。需要读取 rs1 的指令包括 I 型、R 型、S 型、B 型、L 型、JR 型、乘法指令和除法指令。需要读取 rs2 的指令包括 R 型、S 型、B 型、乘法指令和除法指令。

**ID/EX 寄存器更新：**

ID/EX 寄存器在时钟边沿更新，将解码后的信息传递给 EX 阶段。更新逻辑考虑了 ID/EX 有效标志和 IF/ID 有效标志的影响。当 ID/EX 有效标志为 1 且 IF/ID 有效标志为 1 时，ID/EX 寄存器被更新为当前解码的信息；当 ID/EX 有效标志为 1 且 IF/ID 有效标志为 0 时，ID/EX 寄存器被清零；当 ID/EX 有效标志为 0 时，ID/EX 寄存器保持不变。

**输出信号生成：**

ID 阶段的输出信号包括控制信号、寄存器索引、立即数、寄存器需求和预测信息。输出信号的生成逻辑与 IF/ID 寄存器更新逻辑类似，考虑了 ID/EX 有效标志和 IF/ID 有效标志的影响。

### 3.3 EX 阶段（执行）

EX 阶段是流水线的第三个阶段，负责执行 ALU 操作、分支判断、预测验证、乘除法运算，并生成前递数据。该阶段是整个流水线中最复杂的阶段，包含了多种功能单元。

**数据前递：**

EX 阶段首先执行数据前递操作，以解决数据冒险。数据前递的目的是将前序指令的执行结果直接传递给当前指令，避免流水线暂停。

前递逻辑检查 MEM 阶段和 WB 阶段的指令是否写入了当前指令需要的源寄存器。如果 MEM 阶段的指令写入了当前指令的 rs1 或 rs2，则将 MEM 阶段的 ALU 结果前递到当前指令；如果 WB 阶段的指令写入了当前指令的 rs1 或 rs2，则将 WB 阶段的数据（可能是内存数据或 ALU 结果）前递到当前指令。

对于 WB 阶段的前递，需要特别处理 Load 指令的数据。Load 指令从内存读取数据后，需要根据加载类型（LB、LH、LW、LBU、LHU）进行符号扩展或零扩展。EX 阶段内联了 Load 数据处理逻辑，包括字节选择、半字选择、符号扩展和零扩展。

**ALU 输入选择：**

ALU 有两个输入：输入 A 和输入 B。输入 A 的选择取决于 ALU 输入选择信号。当 ALU 输入选择为 0 或 1 时，输入 A 来自 rs1_data；当 ALU 输入选择为 2 时，输入 A 来自 PC；对于 LUI 指令，输入 A 被强制为零；对于 JAL 和 JALR 指令，输入 A 被设置为 PC（用于计算返回地址）。

输入 B 的选择也取决于 ALU 输入选择信号。当 ALU 输入选择为 0 时，输入 B 来自 rs2_data；当 ALU 输入选择为 1 或 2 时，输入 B 来自立即数；对于 JAL 和 JALR 指令，输入 B 被设置为 4（用于计算返回地址 PC+4）。

**ALU 操作：**

ALU 单元执行各种算术和逻辑运算，包括加法、减法、逻辑运算（与、或、异或）、移位（左移、逻辑右移、算术右移）和比较（有符号小于、无符号小于）。

ALU 单元采用共享运算器的设计，以减少硬件资源。加法和减法共享一个加法器，通过选择输入 B 的原值或补码来实现加法或减法。移位操作共享移位量，通过选择不同的移位方向来实现逻辑左移、逻辑右移和算术右移。比较操作共享比较器，通过选择不同的比较结果来实现有符号小于和无符号小于。

**分支判断：**

分支单元负责判断分支指令是否应该跳转。分支单元根据分支操作码和两个源操作数的值，计算实际跳转标志。

分支操作码包括六种：BEQ（等于跳转）、BNE（不等于跳转）、BLT（小于跳转）、BGE（大于等于跳转）、BLTU（无符号小于跳转）、BGEU（无符号大于等于跳转）。

分支单元预计算比较结果，包括等于、有符号小于和无符号小于。然后根据分支操作码选择相应的比较结果作为实际跳转标志。

**预测验证：**

预测验证逻辑判断 BTB 预测是否正确。预测验证考虑两种情况：BTB 命中和 BTB 未命中。

当 BTB 命中时，预测正确的条件是预测跳转标志等于实际跳转标志，且预测的 PC 等于正确的 PC。正确的 PC 是根据实际跳转标志计算的：如果实际跳转，正确的 PC 是分支目标地址；否则正确的 PC 是 PC 加 4。

当 BTB 未命中时，预测正确的条件是实际不跳转。这是因为 BTB 未命中意味着之前没有执行过该分支指令，默认预测不跳转。

如果预测错误，生成预测错误标志，用于触发流水线刷新。

**乘法器执行：**

乘法器采用 Wallace Tree 架构，需要 3 个周期完成。乘法器的执行过程分为三个周期：

第一周期：符号转换和部分积生成。首先根据乘法指令类型确定操作数的符号属性，然后提取符号位，计算结果的符号。如果操作数为负数，则取补码得到绝对值。然后对绝对值进行零扩展到 64 位，生成 32 个部分积。最后通过多级 CSA（Carry-Save Adder）压缩，将 32 个部分积压缩到 4 个 64 位中间值。

第二周期：继续 CSA 压缩。从第一周期保存的 4 个 64 位中间值开始，通过两级 CSA 压缩，最终得到 2 个 64 位中间值（sum 和 carry）。

第三周期：最终加法和结果选择。将 sum 和 carry 相加得到 64 位无符号乘法结果。然后根据结果符号进行修正：如果结果应该为负数，则对 64 位结果取补码。最后根据乘法指令类型选择高 32 位或低 32 位作为最终结果。

乘法器在执行期间会设置乘法器忙标志，阻止新的乘法指令启动。当乘法完成时，设置乘法器完成标志，并将结果传递给 MEM 阶段。

**除法器执行：**

除法器采用 Radix-4 SRT 算法，需要 18 个周期完成。除法器的执行过程分为三个阶段：初始化、迭代和最终修正。

初始化阶段：首先检查除零，如果除数为零，直接返回特殊值（除法指令返回 -1，取余指令返回被除数）。否则，根据指令类型确定商和余数的符号，然后对被除数和除数取绝对值。最后设置初始余数、正商累积器、负商累积器和迭代计数器，进入迭代阶段。

迭代阶段：执行 16 次迭代，每次迭代产生 2 位商。每次迭代的步骤如下：首先将部分余数左移 2 位（乘以 4），然后根据部分余数的高位和除数选择商数字（-2、-1、0、1 或 2），接着更新部分余数为左移后的余数减去商数字乘以除数，最后更新正商累积器和负商累积器。

最终修正阶段：计算最终商为正商累积器减去负商累积器。计算最终余数为部分余数的高 32 位。如果最终余数为负，需要修正商和余数：商减 1，余数加上除数。如果修正后的余数仍然大于等于除数，需要进一步修正：商加 1，余数减去除数。最后根据商和余数的符号进行符号修正。

除法器在执行期间会设置除法器忙标志，阻止新的除法指令启动。当除法完成时，设置除法器完成标志，并将结果传递给 MEM 阶段。

**ALU 结果选择：**

EX 阶段需要选择最终的 ALU 结果。选择优先级为：除法结果 > 乘法结果 > 普通 ALU 结果。

当除法完成时，使用除法结果；当乘法完成时，使用乘法结果；否则使用普通 ALU 结果。普通 ALU 结果根据指令类型确定：对于分支指令，ALU 结果为 0；对于跳转指令，ALU 结果为 PC 加 4（返回地址）；对于其他指令，ALU 结果为 ALU 单元的输出。

**PC 更新和目标地址计算：**

EX 阶段计算正确的 PC 和目标地址。正确的 PC 根据实际跳转标志确定：如果实际跳转，正确的 PC 是分支目标地址；否则正确的 PC 是 PC 加 4。

目标地址根据指令类型确定：对于分支指令和 JAL 指令，目标地址是 PC 加上立即数；对于 JALR 指令，目标地址是 rs1_data 加上立即数，并将最低位清零（保证指令对齐）。

**需要刷新判断：**

需要刷新的情况包括预测错误、JAL 指令和 JALR 指令。当这些情况发生时，需要刷新流水线，清除 IF 和 ID 阶段的指令，并从正确的目标地址重新开始执行。

**EX/MEM 寄存器更新：**

EX/MEM 寄存器在时钟边沿更新，将执行阶段的信息传递给 MEM 阶段。更新逻辑考虑了乘除法指令的特殊情况。当乘除法指令未完成时，EX/MEM 寄存器被清零（插入气泡）；当乘除法指令完成时，使用保存的控制信息和 PC 值更新 EX/MEM 寄存器；对于普通指令，正常更新 EX/MEM 寄存器。

**输出信号生成：**

EX 阶段的输出信号包括 PC 变化标志、目标 PC、控制信号、预测结果、乘法器状态和除法器状态。输出信号的生成逻辑与 EX/MEM 寄存器更新逻辑类似，考虑了乘除法指令的特殊情况。

### 3.4 MEM 阶段（内存访问）

MEM 阶段是流水线的第四个阶段，负责处理内存读写操作，包括字读写、半字读写和字节读写。该阶段还实现了读-修改-写机制，以支持字节和半字存储操作。

**地址计算：**

MEM 阶段首先从 EX 阶段接收 ALU 结果作为内存地址。由于 SRAM 是按字编址的，需要将地址右移 2 位得到字地址。同时提取地址的低 2 位作为字节偏移，用于字节和半字操作。

**存储类型判断：**

根据控制信号中的存储类型字段，判断当前指令是 SB（存储字节）、SH（存储半字）还是 SW（存储字）。SB 和 SH 指令需要读-修改-写操作，而 SW 指令可以直接写入。

**SB/SH 状态机：**

SB/SH 状态机控制读-修改-写操作。状态机有三种状态：IDLE（空闲）、READ（读取）和 WRITE（写入）。

在 IDLE 状态，如果检测到 SB 或 SH 指令，则保存当前指令的地址、数据和存储类型，然后进入 WRITE 状态。在下一个周期，状态机执行写入操作，然后返回 IDLE 状态。

**SRAM 控制信号计算：**

MEM 阶段计算 SRAM 的控制信号，包括写使能、读使能、地址和写数据。

写使能在以下情况为 1：RMW WRITE 阶段（SB/SH 的写入阶段）或 SW 写入阶段。

读使能在以下情况为 1：RMW READ 阶段（SB/SH 的读取阶段）或 Load 读取阶段。

地址根据操作类型确定：RMW WRITE 阶段使用保存的字地址，其他情况使用当前的字地址。

写数据根据操作类型确定：RMW WRITE 阶段使用合并后的写数据，SW 写入阶段使用原始数据。

**读-修改-写数据计算：**

对于 SB 指令，需要根据字节偏移选择要修改的字节，并将其他字节保持不变。SB 写入数据根据字节偏移将数据字节放在正确的位置，其他位置填充 0。SB 掩码根据字节偏移将对应字节的位置设为 0，其他位置设为 1。合并后的写数据为原始数据与掩码相与，然后与写入数据相或。

对于 SH 指令，需要根据字节偏移选择要修改的半字，并将其他半字保持不变。SH 写入数据根据字节偏移将数据半字放在正确的位置，其他位置填充 0。SH 掩码根据字节偏移将对应半字的位置设为 0，其他位置设为 1。合并后的写数据为原始数据与掩码相与，然后与写入数据相或。

**MEM/WB 寄存器更新：**

MEM/WB 寄存器在时钟边沿更新，将内存访问阶段的信息传递给 WB 阶段。更新逻辑考虑了 SB/SH 状态机的影响。在 IDLE 状态且不是 SB/SH 开始时，正常更新 MEM/WB 寄存器；在 WRITE 状态，正常更新 MEM/WB 寄存器；在 IDLE 状态且是 SB/SH 开始时，MEM/WB 寄存器被清零（不写回寄存器）。

**输出信号生成：**

MEM 阶段的输出信号包括控制信号和 SB/SH 活动标志。SB/SH 活动标志用于指示当前是否正在进行 SB/SH 操作，该标志被传递给 HazardUnit 用于生成暂停信号。

### 3.5 WB 阶段（写回）

WB 阶段是流水线的最后一个阶段，负责将执行结果写回寄存器文件。该阶段处理 Load 数据的符号扩展和零扩展，并根据控制信号选择写回数据。

**Load 数据处理：**

对于 Load 指令，需要根据加载类型对从内存读取的数据进行处理。加载类型包括 LB（加载有符号字节）、LH（加载有符号半字）、LW（加载字）、LBU（加载无符号字节）、LHU（加载无符号半字）。

首先根据地址的低 2 位选择对应的字节或半字。对于字节加载，根据字节偏移选择对应的字节；对于半字加载，根据字节偏移的最高位选择对应的半字。

然后根据加载类型进行符号扩展或零扩展。对于 LB，将选中的字节进行符号扩展到 32 位；对于 LBU，将选中的字节进行零扩展到 32 位；对于 LH，将选中的半字进行符号扩展到 32 位；对于 LHU，将选中的半字进行零扩展到 32 位；对于 LW，直接使用 32 位数据。

**写回数据选择：**

根据控制信号中的内存到寄存器标志，选择写回数据。如果内存到寄存器标志为 1，写回数据为处理后的内存数据；否则写回数据为 EX 阶段的 ALU 结果。

**寄存器写回：**

如果控制信号中的寄存器写使能为 1，则将写回数据写入目标寄存器。目标寄存器的索引从控制信号中获取。需要注意的是，x0 寄存器是硬连线为 0 的，不应该被写入。

**输出信号生成：**

WB 阶段的输出信号主要是控制信号，用于传递给 HazardUnit 进行冒险检测。WB 阶段不产生其他输出信号，因为写回操作是最后一个流水线阶段。

---

## 4. 级间通信和数据传输

### 4.1 流水线寄存器更新机制

流水线寄存器是连接各个流水线阶段的桥梁，负责在时钟边沿捕获前一阶段的输出，并在下一个周期提供给下一阶段。流水线寄存器的更新机制是流水线正常运行的关键。

**更新条件控制：**

流水线寄存器的更新不是无条件进行的，而是受到多种条件的影响。主要的控制条件包括有效标志、暂停信号和刷新信号。

有效标志指示当前流水线寄存器中的指令是否有效。如果有效标志为 0，表示当前寄存器中的指令是气泡（NOP），不应该影响后续阶段。

暂停信号指示当前周期是否需要暂停流水线。当暂停信号为 1 时，IF 阶段和 ID 阶段停止更新，EX、MEM、WB 阶段继续执行。

刷新信号指示当前周期是否需要刷新流水线。当刷新信号为 1 时，IF/ID 寄存器被清零，PC 被设置为正确的目标地址。

**IF/ID 寄存器更新：**

IF/ID 寄存器的更新逻辑如下：当 IF/ID 有效标志为 1 且暂停信号为 0 时，IF/ID 寄存器被更新为当前获取的指令和相关信息；当 IF/ID 有效标志为 1 且暂停信号为 1 时，IF/ID 寄存器被清零；当 IF/ID 有效标志为 0 时，IF/ID 寄存器保持不变。

**ID/EX 寄存器更新：**

ID/EX 寄存器的更新逻辑更加复杂，需要考虑多种情况。主要的控制变量包括 ID/EX 有效标志、IF/ID 有效标志、暂停信号、刷新信号和乘除法执行状态。

当需要刷新时，ID/EX 寄存器被清零；当需要插入气泡时，ID/EX 寄存器被清零；当乘除法执行中时，ID/EX 寄存器保持不变（等待乘除法完成）；否则，ID/EX 寄存器被更新为当前解码的信息。

**EX/MEM 寄存器更新：**

EX/MEM 寄存器的更新逻辑需要考虑乘除法指令的特殊情况。当乘除法指令未完成时，EX/MEM 寄存器被清零（插入气泡）；当乘除法指令完成时，使用保存的控制信息和 PC 值更新 EX/MEM 寄存器；对于普通指令，正常更新 EX/MEM 寄存器。

**MEM/WB 寄存器更新：**

MEM/WB 寄存器的更新逻辑需要考虑 SB/SH 状态机的影响。在 IDLE 状态且不是 SB/SH 开始时，正常更新 MEM/WB 寄存器；在 WRITE 状态，正常更新 MEM/WB 寄存器；在 IDLE 状态且是 SB/SH 开始时，MEM/WB 寄存器被清零（不写回寄存器）。

### 4.2 数据前递实现

数据前递是解决数据冒险的关键技术。当后续指令需要使用前序指令的执行结果时，数据前递机制将结果直接从 MEM 阶段或 WB 阶段传递给 EX 阶段，避免了流水线暂停。

**前递来源：**

数据前递有两个主要来源：MEM 阶段和 WB 阶段。

MEM 阶段前递的数据是 ALU 执行结果。当 MEM 阶段的指令写入了当前指令需要的源寄存器时，将 MEM 阶段的 ALU 结果前递到当前指令。

WB 阶段前递的数据可能是内存数据或 ALU 结果。当 WB 阶段的指令写入了当前指令需要的源寄存器时，根据内存到寄存器标志选择前递数据。如果内存到寄存器标志为 1，前递处理后的内存数据；否则前递 ALU 结果。

**前递条件判断：**

前递条件判断需要检查多个因素：前递指令的有效性、前递指令的写使能、目标寄存器的有效性以及寄存器索引的匹配。

对于 MEM 阶段前递，条件包括：EX/MEM 有效标志为 1、MEM 阶段的寄存器写使能为 1、MEM 阶段的目标寄存器不为 0、当前指令的源寄存器索引等于 MEM 阶段的目标寄存器索引。

对于 WB 阶段前递，条件包括：MEM/WB 有效标志为 1、WB 阶段的寄存器写使能为 1、WB 阶段的目标寄存器不为 0、当前指令的源寄存器索引等于 WB 阶段的目标寄存器索引。

**前递优先级：**

当 MEM 阶段和 WB 阶段都满足前递条件时，MEM 阶段的前递优先级高于 WB 阶段。这是因为 MEM 阶段的结果是最新的，而 WB 阶段的结果是上一个周期的。

前递优先级的实现方式是：首先尝试从 MEM 阶段前递，如果 MEM 阶段不满足前递条件，则尝试从 WB 阶段前递，如果 WB 阶段也不满足前递条件，则从寄存器文件读取数据。

**Load 数据前递：**

Load 指令的数据前递需要特殊处理。Load 指令从内存读取数据后，需要根据加载类型进行符号扩展或零扩展。WB 阶段前递 Load 数据时，需要内联处理 Load 数据。

Load 数据处理包括以下步骤：首先根据地址的低 2 位选择对应的字节或半字；然后根据加载类型进行符号扩展或零扩展；最后将处理后的数据作为前递数据。

**前递数据选择：**

前递数据选择逻辑如下：首先从寄存器文件读取基础数据；然后检查 WB 阶段前递条件，如果满足则用 WB 阶段的数据替换基础数据；接着检查 MEM 阶段前递条件，如果满足则用 MEM 阶段的数据替换当前数据。

### 4.3 冒险检测和处理

冒险检测和处理是流水线正确运行的关键。HazardUnit 模块负责检测各种冒险情况，并生成相应的控制信号。

**Load-Use 冒险：**

Load-Use 冒险是指一条加载指令后紧跟一条需要使用加载结果的指令。由于加载指令需要等到 MEM 阶段才能从内存读取数据，而后续指令在 EX 阶段就需要数据，因此存在数据冒险。

Load-Use 冒险的检测条件包括：MEM 阶段的内存读使能为 1、MEM 阶段的寄存器写使能为 1、MEM 阶段的目标寄存器不为 0、当前指令需要读取源寄存器且源寄存器索引等于 MEM 阶段的目标寄存器索引。

Load-Use 冒险的处理方式是插入流水线气泡，暂停 IF 和 ID 阶段一个周期，让加载指令完成内存访问后再执行后续指令。

**乘除法冒险：**

乘除法冒险是指乘除法指令需要多个周期完成，期间流水线需要暂停。乘法器需要 3 个周期完成，除法器需要 18 个周期完成。

乘除法冒险的检测条件包括：乘除法器正在执行中、当前指令需要读取源寄存器且源寄存器索引等于乘除法指令的目标寄存器索引。

乘除法冒险的处理方式是暂停 IF 和 ID 阶段，等待乘除法指令完成。在乘除法指令完成期间，ID/EX 寄存器保持不变，保存乘除法指令的信息。

**控制冒险：**

控制冒险是指分支和跳转指令导致的 PC 变化。控制冒险包括分支预测错误、JAL 指令和 JALR 指令。

控制冒险的检测条件包括：预测错误标志为 1、跳转指令标志为 1、寄存器跳转标志为 1。

控制冒险的处理方式是刷新流水线，清除 IF 和 ID 阶段的指令，并从正确的目标地址重新开始执行。

**SB/SH 冒险：**

SB/SH 冒险是指字节存储和半字存储需要两个周期完成读-修改-写操作，期间流水线需要暂停。

SB/SH 冒险的检测条件包括：MEM 阶段的内存写使能为 1、存储类型为 SB 或 SH。

SB/SH 冒险的处理方式是暂停 IF、ID 和 EX 阶段，等待 SB/SH 操作完成。在 SB/SH 操作期间，EX/MEM 寄存器保持不变。

**综合暂停逻辑：**

综合暂停逻辑考虑所有冒险情况，生成最终的暂停信号。暂停信号在以下情况为 1：Load-Use 冒险、乘除法执行中、乘除法结果冒险、除法执行中、除法结果冒险、SB/SH 活动中。

暂停信号为 1 时，IF 和 ID 阶段停止更新，EX、MEM、WB 阶段继续执行。暂停信号由 HazardUnit 生成，通过 stall 寄存器传递到 IF 阶段。

---

## 5. BTB分支预测器实现

### 5.1 BTB表结构

分支目标缓冲区（Branch Target Buffer，BTB）是一个用于缓存分支指令目标地址的结构。BTB 的核心思想是利用程序的空间局部性，假设之前执行过的分支指令会再次执行，并且跳转到相同的目标地址。

**BTB 表项设计：**

BTB 包含 64 个表项，每个表项存储一个分支指令的预测信息。BTB 使用 PC 的低 6 位（第 2 到 7 位）作为索引，因为分支指令通常是 4 字节对齐的，所以 PC 的最低两位总是 0。

每个 BTB 表项包含以下信息：

- 目标地址：32 位，存储分支指令的目标 PC 值
- 有效标志：1 位，指示该 BTB 表项是否有效

**BTB 索引计算：**

BTB 索引从当前 PC 中提取。由于指令是 4 字节对齐的，PC 的最低两位总是 0，因此使用 PC 的第 2 到 7 位作为索引。索引计算公式为：btb_index = pc[2:7]。

**BTB 初始化：**

BTB 的所有表项初始化为无效状态，有效标志为 0，目标地址为 0。这意味着初始时所有分支指令都会预测不跳转。

### 5.2 BHT（2-bit饱和计数器）

分支历史表（Branch History Table，BHT）使用 2 位饱和计数器来记录分支的历史行为，用于预测分支的跳转方向。

**2 位饱和计数器原理：**

2 位饱和计数器有四种状态：00（Strongly Not Taken，强不跳转）、01（Weakly Not Taken，弱不跳转）、10（Weakly Taken，弱跳转）、11（Strongly Taken，强跳转）。

计数器的更新规则如下：

- 当分支实际跳转时，计数器加 1，但不超过 3（饱和）
- 当分支实际不跳转时，计数器减 1，但不低于 0（饱和）

**预测规则：**

根据计数器的值预测分支的跳转方向：

- 当计数器值大于等于 2（状态 10 或 11）时，预测分支跳转
- 当计数器值小于 2（状态 00 或 01）时，预测分支不跳转

**BHT 初始化：**

BHT 的所有表项初始化为 01（Weakly Not Taken），这意味着初始时预测分支不跳转，但容易改变预测方向。

### 5.3 预测逻辑

预测逻辑在 IF 阶段执行，用于预测分支指令的跳转方向和目标地址。

**BTB 查询：**

IF 阶段首先根据当前 PC 计算 BTB 索引，然后读取对应的 BTB 表项和 BHT 表项。BTB 命中判断直接使用 BTB 有效标志。

**预测跳转方向：**

预测跳转方向根据 BHT 值确定。当 BHT 值大于等于 2 时，预测跳转；否则预测不跳转。

**预测目标地址：**

预测目标地址根据 BTB 命中标志和预测跳转标志确定。当 BTB 命中且预测跳转时，预测目标地址为 BTB 中存储的目标地址；否则预测目标地址为 PC 加 4。

**预测信息打包：**

预测信息被打包成一个 34 位的向量，包括 BTB 命中标志、预测跳转标志和预测的 PC 值。预测信息通过 IF/ID 寄存器传递给 ID 阶段，然后通过 ID/EX 寄存器传递给 EX 阶段。

### 5.4 预测更新机制

预测更新机制在 EX 阶段执行，用于根据分支指令的实际执行结果更新 BTB 和 BHT。

**预测验证：**

EX 阶段首先验证预测是否正确。预测验证考虑两种情况：BTB 命中和 BTB 未命中。

当 BTB 命中时，预测正确的条件是预测跳转标志等于实际跳转标志，且预测的 PC 等于正确的 PC。正确的 PC 是根据实际跳转标志计算的：如果实际跳转，正确的 PC 是分支目标地址；否则正确的 PC 是 PC 加 4。

当 BTB 未命中时，预测正确的条件是实际不跳转。这是因为 BTB 未命中意味着之前没有执行过该分支指令，默认预测不跳转。

**BTB 更新：**

当分支指令执行完成时，需要更新 BTB。BTB 更新包括设置目标地址和有效标志。

目标地址更新为实际的目标地址。对于分支指令，目标地址是 PC 加上立即数；对于 JAL 指令，目标地址是 PC 加上立即数；对于 JALR 指令，目标地址是 rs1 加上立即数，并将最低位清零。

有效标志设置为 1，表示该 BTB 表项现在有效。

**BHT 更新：**

BHT 更新根据分支的实际跳转方向进行。当分支实际跳转时，BHT 值加 1，但不超过 3；当分支实际不跳转时，BHT 值减 1，但不低于 0。

BHT 更新使用饱和计数器机制，确保计数器值始终在 0 到 3 之间。

**预测错误恢复：**

当预测错误时，需要刷新流水线并从正确的目标地址重新开始执行。预测错误恢复包括以下步骤：

1. 设置预测错误标志
2. 刷新 IF/ID 寄存器，清除正在 IF 和 ID 阶段的指令
3. 更新 PC 到正确的目标地址
4. 更新 BTB 和 BHT，提高未来的预测准确率

**预测准确率优化：**

2 位饱和计数器机制可以平滑分支行为的变化，提高预测准确率。当分支行为改变时，计数器需要两次错误预测才能改变预测方向，这避免了偶然的分支行为变化导致预测频繁翻转。

BTB 的 64 个表项提供了足够的缓存容量，可以缓存大多数循环和条件分支的目标地址。BTB 的索引使用 PC 的低 6 位，利用了程序的空间局部性。

---

## 6. Forwarding实现

### 6.1 Forwarding概述

数据前递（Data Forwarding）是解决数据冒险的关键技术。在五级流水线中，当一条指令需要使用前一条指令的执行结果时，由于结果还没有写回寄存器文件，直接读取寄存器文件会得到错误的数据。数据前递机制将结果直接从 MEM 阶段或 WB 阶段传递给 EX 阶段，避免了流水线暂停。

**数据冒险类型：**

数据冒险主要有两种类型：RAW（Read After Write）冒险和 WAW（Write After Write）冒险。RAW 冒险是指后续指令需要使用前序指令的写回数据；WAW 冒险是指两条指令写入同一个寄存器，但后续指令先完成。在五级流水线中，主要关注 RAW 冒险。

**前递路径：**

前递路径主要有两条：EX 阶段从 MEM 阶段前递，EX 阶段从 WB 阶段前递。MEM 阶段的前递数据是 ALU 执行结果，WB 阶段的前递数据可能是内存数据或 ALU 结果。

**前递时机：**

前递在 EX 阶段执行，当 EX 阶段需要读取源寄存器时，首先检查 MEM 阶段和 WB 阶段是否写入了需要的寄存器。如果满足前递条件，则使用前递数据；否则从寄存器文件读取数据。

### 6.2 MEM阶段前递

MEM 阶段前递是指将 MEM 阶段的 ALU 执行结果直接传递给 EX 阶段。MEM 阶段的前递数据是最新的，因为 MEM 阶段是 EX 阶段的直接后继。

**前递条件：**

MEM 阶段前递的条件包括：EX/MEM 有效标志为 1、MEM 阶段的寄存器写使能为 1、MEM 阶段的目标寄存器不为 0、当前指令的源寄存器索引等于 MEM 阶段的目标寄存器索引。

EX/MEM 有效标志为 1 表示 MEM 阶段有有效指令；MEM 阶段的寄存器写使能为 1 表示 MEM 阶段的指令会写回寄存器；MEM 阶段的目标寄存器不为 0 表示目标寄存器不是 x0（x0 是硬连线为 0 的，不应该被前递）；源寄存器索引等于目标寄存器索引表示当前指令需要使用 MEM 阶段指令的写回数据。

**前递数据：**

MEM 阶段的前递数据是 ALU 执行结果，存储在 EX/MEM 寄存器中。ALU 执行结果可能是算术运算结果、逻辑运算结果、移位结果或比较结果。

**前递实现：**

MEM 阶段前递的实现方式是：首先从寄存器文件读取基础数据；然后检查 MEM 阶段前递条件，如果满足则用 MEM 阶段的 ALU 结果替换基础数据。

### 6.3 WB阶段前递

WB 阶段前递是指将 WB 阶段的写回数据直接传递给 EX 阶段。WB 阶段的前递数据可能是内存数据或 ALU 结果，需要根据内存到寄存器标志选择。

**前递条件：**

WB 阶段前递的条件包括：MEM/WB 有效标志为 1、WB 阶段的寄存器写使能为 1、WB 阶段的目标寄存器不为 0、当前指令的源寄存器索引等于 WB 阶段的目标寄存器索引。

MEM/WB 有效标志为 1 表示 WB 阶段有有效指令；WB 阶段的寄存器写使能为 1 表示 WB 阶段的指令会写回寄存器；WB 阶段的目标寄存器不为 0 表示目标寄存器不是 x0；源寄存器索引等于目标寄存器索引表示当前指令需要使用 WB 阶段指令的写回数据。

**前递数据选择：**

WB 阶段的前递数据根据内存到寄存器标志选择。如果内存到寄存器标志为 1，前递处理后的内存数据；否则前递 ALU 结果。

内存到寄存器标志为 1 表示当前指令是 Load 指令，写回数据来自内存；内存到寄存器标志为 0 表示当前指令不是 Load 指令，写回数据来自 ALU 结果。

**前递实现：**

WB 阶段前递的实现方式是：首先从寄存器文件读取基础数据；然后检查 WB 阶段前递条件，如果满足则根据内存到寄存器标志选择前递数据，替换基础数据。

### 6.4 Load数据处理和前递

Load 指令的数据前递需要特殊处理。Load 指令从内存读取数据后，需要根据加载类型进行符号扩展或零扩展。WB 阶段前递 Load 数据时，需要内联处理 Load 数据。

**Load 类型：**

Load 指令有五种类型：LB（加载有符号字节）、LH（加载有符号半字）、LW（加载字）、LBU（加载无符号字节）、LHU（加载无符号半字）。

**字节选择：**

对于字节加载（LB 和 LBU），需要根据地址的低 2 位选择对应的字节。地址的低 2 位表示字节偏移，0 表示最低字节，1 表示次低字节，2 表示次高字节，3 表示最高字节。

**半字选择：**

对于半字加载（LH 和 LHU），需要根据地址的低 2 位选择对应的半字。地址的低 2 位的最高位表示半字偏移，0 表示低半字，1 表示高半字。

**符号扩展：**

对于有符号字节加载（LB），将选中的字节进行符号扩展到 32 位。符号扩展时，使用字节的最高位作为符号位，高 24 位用符号位填充。

对于有符号半字加载（LH），将选中的半字进行符号扩展到 32 位。符号扩展时，使用半字的最高位作为符号位，高 16 位用符号位填充。

**零扩展：**

对于无符号字节加载（LBU），将选中的字节进行零扩展到 32 位。零扩展时，高 24 位填充 0。

对于无符号半字加载（LHU），将选中的半字进行零扩展到 32 位。零扩展时，高 16 位填充 0。

**Load 数据处理实现：**

Load 数据处理的实现方式是：首先根据地址的低 2 位选择对应的字节或半字；然后根据加载类型进行符号扩展或零扩展；最后将处理后的数据作为前递数据。

### 6.5 前递优先级

当 MEM 阶段和 WB 阶段都满足前递条件时，需要确定前递优先级。MEM 阶段的前递优先级高于 WB 阶段，因为 MEM 阶段的结果是最新的。

**优先级规则：**

前递优先级规则如下：首先尝试从 MEM 阶段前递，如果 MEM 阶段不满足前递条件，则尝试从 WB 阶段前递，如果 WB 阶段也不满足前递条件，则从寄存器文件读取数据。

**优先级实现：**

前递优先级的实现方式是：首先从寄存器文件读取基础数据；然后检查 WB 阶段前递条件，如果满足则用 WB 阶段的数据替换基础数据；接着检查 MEM 阶段前递条件，如果满足则用 MEM 阶段的数据替换当前数据。

这种实现方式确保了 MEM 阶段的前递优先级高于 WB 阶段，因为 MEM 阶段的检查在 WB 阶段之后执行，会覆盖 WB 阶段的前递数据。

**前递冲突处理：**

当 MEM 阶段和 WB 阶段都满足前递条件，但前递数据不同时，需要选择正确的数据。由于 MEM 阶段的结果是最新的，应该优先使用 MEM 阶段的前递数据。

前递冲突通常发生在 Load-Use 冒险的情况下，即一条 Load 指令后紧跟一条需要使用 Load 结果的指令。这种情况下，Load 指令在 MEM 阶段从内存读取数据，后续指令在 EX 阶段需要使用 Load 结果。由于 Load 数据还没有写回寄存器文件，需要从 MEM 阶段前递。

**Load-Use 冒险处理：**

Load-Use 冒险是指一条 Load 指令后紧跟一条需要使用 Load 结果的指令。由于 Load 指令需要等到 MEM 阶段才能从内存读取数据，而后续指令在 EX 阶段就需要数据，因此存在数据冒险。

Load-Use 冒险的处理方式是插入流水线气泡，暂停 IF 和 ID 阶段一个周期，让 Load 指令完成内存访问后再执行后续指令。在暂停期间，Load 指令从 MEM 阶段移动到 WB 阶段，Load 数据被写回寄存器文件。后续指令可以从寄存器文件读取 Load 数据，或者从 WB 阶段前递 Load 数据。

**前递和暂停的配合：**

前递和暂停的配合可以解决大多数数据冒险。对于 Load-Use 冒险，使用暂停机制；对于其他数据冒险，使用前递机制。这种配合可以最大限度地减少流水线停顿，提高性能。

---

## 7. 乘法器实现

### 7.1 Wallace Tree架构

Wallace Tree 是一种高效的并行乘法器架构，通过多级 Carry-Save Adder（CSA）压缩技术，将乘法的延迟压缩到最短。Wallace Tree 的核心思想是通过并行压缩减少关键路径的延迟，相比传统的阵列乘法器具有更好的性能。

**Wallace Tree 原理：**

Wallace Tree 将乘法分解为两个步骤：部分积生成和部分积压缩。部分积生成阶段生成所有部分积，每个部分积是被乘数乘以乘数的一位。部分积压缩阶段通过多级 CSA 将所有部分积压缩为两个数（sum 和 carry），最后通过一个加法器得到最终结果。

**CSA 压缩：**

CSA（Carry-Save Adder）是一种特殊的加法器，它接受三个输入，产生两个输出：sum 和 carry。sum 是三个输入的异或，carry 是三个输入的多数函数左移一位。

CSA 的数学表达式为：sum = a ^ b ^ c，carry = ((a & b) | (b & c) | (a & c)) << 1。

CSA 的优势在于它不需要等待进位传播，可以并行处理多个部分积，从而减少关键路径的延迟。

**Wallace Tree 压缩过程：**

Wallace Tree 的压缩过程分为多级，每级将部分积的数量减少。对于 32 位乘法，初始有 32 个部分积。第一级 CSA 将 32 个部分积压缩到 22 个，第二级压缩到 15 个，第三级压缩到 10 个，第四级压缩到 7 个，第五级压缩到 5 个，第六级压缩到 4 个，第七级压缩到 3 个，第八级压缩到 2 个。

最终的两个数（sum 和 carry）通过一个普通加法器相加，得到最终的乘法结果。

### 7.2 3周期流水线设计

乘法器采用 3 周期流水线设计，将乘法操作分解为三个阶段，每个阶段在一个时钟周期内完成。

**周期划分：**

第一周期：符号转换、部分积生成和第一级 CSA 压缩。这个周期完成符号转换法的前两步（确定符号属性和提取符号位）、生成 32 个部分积，并通过前四级 CSA 将部分积从 32 个压缩到 4 个。

第二周期：继续 CSA 压缩。这个周期通过后四级 CSA 将部分积从 4 个压缩到 2 个（sum 和 carry）。

第三周期：最终加法和结果选择。这个周期将 sum 和 carry 相加得到 64 位无符号乘法结果，根据结果符号进行修正，最后根据乘法指令类型选择高 32 位或低 32 位作为最终结果。

**流水线寄存器：**

乘法器使用多个流水线寄存器来保存中间结果。主要的流水线寄存器包括：

- mul_a：保存乘法操作数 A
- mul_b：保存乘法操作数 B
- mul_op_reg：保存乘法操作码
- mul_cycle_counter：保存乘法周期计数器（0=空闲，1/2/3=执行中）
- mul_stage1_sum：保存第一级 CSA 压缩结果（sum）
- mul_stage1_carry：保存第一级 CSA 压缩结果（carry）
- mul_stage2_sum：保存第二级 CSA 压缩结果（sum）
- mul_stage2_carry：保存第二级 CSA 压缩结果（carry）
- mul_result_reg：保存乘法结果
- mul_valid：保存乘法结果有效标志
- mul_in_progress：保存乘法执行中标志
- mul_rd_reg：保存乘法目标寄存器
- mul_control_reg：保存乘法控制信号
- mul_pc_reg：保存乘法指令 PC
- mul_result_sign：保存乘法结果符号

**流水线控制：**

乘法器使用周期计数器来控制流水线的执行。周期计数器的值为 0 表示乘法器空闲，值为 1、2、3 表示乘法器正在执行。

当乘法器空闲且当前指令是乘法指令时，启动新的乘法操作，设置周期计数器为 1，保存操作数和控制信息。

当周期计数器为 1 时，执行第一周期的操作，然后将周期计数器设置为 2。

当周期计数器为 2 时，执行第二周期的操作，然后将周期计数器设置为 3。

当周期计数器为 3 时，执行第三周期的操作，然后将周期计数器设置为 0，设置乘法结果有效标志。

### 7.3 符号转换法

符号转换法是一种处理有符号乘法的技术，通过将操作数转换为绝对值进行无符号乘法，然后根据结果符号进行修正，避免了直接实现有符号乘法的复杂性。

**符号转换法步骤：**

符号转换法分为三个步骤：符号属性确定、绝对值转换和结果符号修正。

**步骤 1：符号属性确定**

根据乘法指令类型确定操作数的符号属性。MUL 和 MULH 指令的两个操作数都是有符号的；MULHSU 指令的第一个操作数是有符号的，第二个操作数是无符号的；MULHU 指令的两个操作数都是无符号的。

**步骤 2：绝对值转换**

提取操作数的符号位，判断操作数是否为负数。如果操作数是有符号类型且符号位为 1，则操作数为负数，需要取补码得到绝对值。补码的计算方法是：对操作数取反，然后加 1。

**步骤 3：结果符号修正**

计算结果的符号，结果符号是两个操作数符号的异或。如果结果符号为 1，则结果应该为负数，需要对 64 位无符号乘法结果取补码。如果结果符号为 0，则结果应该为正数，不需要修正。

对于 MULHU 指令，结果总是正数，不需要符号修正。

**符号转换法实现：**

符号转换法的实现方式是：首先根据乘法指令类型确定操作数的符号属性；然后提取操作数的符号位，判断操作数是否为负数；接着对负数操作数取补码得到绝对值；然后对绝对值进行零扩展到 64 位，生成 32 个部分积；最后通过多级 CSA 压缩得到 64 位无符号乘法结果，根据结果符号进行修正。

### 7.4 CSA压缩过程

CSA 压缩过程是 Wallace Tree 的核心，通过多级 CSA 将 32 个部分积压缩到 2 个数。

**第一级 CSA（32→22）：**

第一级 CSA 将 32 个部分积压缩到 22 个。压缩方式是将每 3 个部分积通过一个 CSA 压缩为 2 个数（sum 和 carry），共 10 组 CSA，压缩 30 个部分积到 20 个，加上剩余的 2 个部分积，总共 22 个。

**第二级 CSA（22→15）：**

第二级 CSA 将 22 个部分积压缩到 15 个。压缩方式是将每 3 个部分积通过一个 CSA 压缩为 2 个数，共 7 组 CSA，压缩 21 个部分积到 14 个，加上剩余的 1 个部分积，总共 15 个。

**第三级 CSA（15→10）：**

第三级 CSA 将 15 个部分积压缩到 10 个。压缩方式是将每 3 个部分积通过一个 CSA 压缩为 2 个数，共 5 组 CSA，压缩 15 个部分积到 10 个。

**第四级 CSA（10→7）：**

第四级 CSA 将 10 个部分积压缩到 7 个。压缩方式是将每 3 个部分积通过一个 CSA 压缩为 2 个数，共 3 组 CSA，压缩 9 个部分积到 6 个，加上剩余的 1 个部分积，总共 7 个。

**第五级 CSA（7→5）：**

第五级 CSA 将 7 个部分积压缩到 5 个。压缩方式是将每 3 个部分积通过一个 CSA 压缩为 2 个数，共 2 组 CSA，压缩 6 个部分积到 4 个，加上剩余的 1 个部分积，总共 5 个。

**第六级 CSA（5→4）：**

第六级 CSA 将 5 个部分积压缩到 4 个。压缩方式是将 3 个部分积通过一个 CSA 压缩为 2 个数，剩余的 2 个部分积保持不变，总共 4 个。

**第七级 CSA（4→3）：**

第七级 CSA 将 4 个部分积压缩到 3 个。压缩方式是将 3 个部分积通过一个 CSA 压缩为 2 个数，剩余的 1 个部分积保持不变，总共 3 个。

**第八级 CSA（3→2）：**

第八级 CSA 将 3 个部分积压缩到 2 个。压缩方式是将 3 个部分积通过一个 CSA 压缩为 2 个数（sum 和 carry）。

**CSA 压缩实现：**

CSA 压缩的实现方式是：定义一个 CSA 函数，接受三个 64 位输入，产生两个 64 位输出（sum 和 carry）。sum 是三个输入的异或，carry 是三个输入的多数函数左移一位。

然后通过多级调用 CSA 函数，将 32 个部分积逐步压缩到 2 个数。每级 CSA 的输入是上一级的输出，输出是下一级的输入。

---

## 8. 除法器实现

### 8.1 Radix-4 SRT算法

Radix-4 SRT（Sweeney-Robertson-Tocher）算法是一种高效的除法算法，每次迭代产生 2 位商，相比传统的恢复除法或不恢复除法，具有更快的执行速度。

**SRT 算法原理：**

SRT 算法是一种不恢复除法算法，它允许部分余数为负数，通过冗余的商数字表示来避免恢复步骤。Radix-4 SRT 算法使用商数字集合为 -2、-1、0、+1、+2，每次迭代产生 2 位商。

**递归关系：**

SRT 算法的递归关系为：P_{i+1} = 4 * P_i - q_i * D，其中 P_i 是第 i 次迭代的部分余数，q_i 是第 i 次迭代的商数字，D 是除数。

**商数字选择：**

商数字的选择基于部分余数的高位和除数的比较。对于 Radix-4 SRT 算法，商数字的选择规则如下：

- q = +2 if P_high >= 2*D
- q = +1 if P_high >= D and P_high < 2*D
- q = 0 if P_high >= 0 and P_high < D (or P_high >= -D and P_high < 0)
- q = -1 if P_high >= -2*D and P_high < -D
- q = -2 if P_high < -2*D

其中 P_high 是部分余数的高 32 位。

**正负累计商：**

SRT 算法使用正负累计商技术。正负累计商使用两个商累积器 Q+ 和 Q-，分别累积正商数字和负商数字的绝对值。最终的商为 Q+ - Q-。

Q+ 和 Q- 的更新规则如下：

- 如果 q >= 0：Q+ = Q+ * 4 + q，Q- = Q- * 4
- 如果 q < 0：Q+ = Q+ * 4，Q- = Q- * 4 + |q|

### 8.2 18周期流水线设计

除法器采用 18 周期流水线设计，将除法操作分解为三个阶段：初始化、迭代和最终修正。

**阶段划分：**

初始化阶段（Cycle 1）：处理符号、检查除零、设置初始余数和商累积器。

迭代阶段（Cycle 2-17）：执行 16 次迭代，每次迭代产生 2 位商。

最终修正阶段（Cycle 18）：计算最终商和余数，进行符号修正和边界修正。

**流水线寄存器：**

除法器使用多个流水线寄存器来保存中间结果。主要的流水线寄存器包括：

- div_dividend：保存被除数（绝对值）
- div_divisor：保存除数（绝对值）
- div_op_reg：保存除法操作码
- div_state：保存除法器状态（0=IDLE，1=INIT，2-17=ITERATE，18=FINAL_CORRECTION，19=DONE）
- div_remainder：保存部分余数 P（有符号，可为负）
- div_quotient_pos：保存正商累积 Q+
- div_quotient_neg：保存负商累积 Q-
- div_iter_count：保存迭代计数器（0-15）
- div_sign：保存商结果符号
- div_dividend_sign：保存被除数原始符号（用于余数符号）
- div_valid：保存除法结果有效标志
- div_result_reg：保存除法结果
- div_rd_reg：保存除法目标寄存器
- div_control_reg：保存除法控制信号
- div_pc_reg：保存除法指令 PC


**流水线控制：**

除法器使用状态机来控制流水线的执行。状态机的状态包括：

- IDLE（0）：除法器空闲，等待新的除法指令
- INIT（1）：初始化阶段
- ITERATE（2-17）：迭代阶段，执行 16 次迭代
- FINAL_CORRECTION（18）：最终修正阶段
- DONE（19）：完成阶段，返回 IDLE 状态

当除法器空闲且当前指令是除法指令时，启动新的除法操作，设置状态为 INIT。

当状态为 INIT 时，执行初始化操作，然后设置状态为 ITERATE。

当状态为 ITERATE 时，执行一次迭代操作，更新迭代计数器。如果迭代计数器达到 15，则设置状态为 FINAL_CORRECTION；否则保持状态为 ITERATE。

当状态为 FINAL_CORRECTION 时，执行最终修正操作，然后设置状态为 DONE。

当状态为 DONE 时，返回 IDLE 状态。

### 8.3 状态机实现

除法器的状态机实现了完整的除法流程，从初始化到最终修正。

**状态 0：IDLE**

IDLE 状态是除法器的空闲状态，等待新的除法指令。在 IDLE 状态，除法器不执行任何操作，只是等待启动信号。

**状态 1：INIT**

INIT 状态是除法器的初始化阶段，主要完成以下操作：

1. 检查除零：如果除数为零，直接返回特殊值（除法指令返回 -1，取余指令返回被除数），然后跳转到 DONE 状态。

2. 处理符号：根据除法指令类型确定商和余数的符号。对于有符号除法，商的符号是被除数符号异或除数符号，余数的符号是被除数符号。

3. 取绝对值：对于有符号操作数，如果符号位为 1，则取补码得到绝对值。

4. 设置初始余数：将被除数放在 64 位的低 32 位，高 32 位填充 0。

5. 设置商累积器：将 Q+ 和 Q- 初始化为 0。

6. 设置迭代计数器：将迭代计数器初始化为 0。

7. 跳转到 ITERATE 状态。

**状态 2-17：ITERATE**

ITERATE 状态是除法器的迭代阶段，执行 16 次迭代，每次迭代产生 2 位商。每次迭代的操作包括：

1. 左移部分余数：将部分余数左移 2 位（乘以 4）。

2. 选择商数字：根据部分余数的高位和除数选择商数字。

3. 计算商数字乘以除数：根据商数字计算 q * D。

4. 更新部分余数：部分余数 = 左移后的余数 - q * D。

5. 更新商累积器：根据商数字的正负更新 Q+ 和 Q-。

6. 更新迭代计数器：迭代计数器加 1。

7. 检查是否完成：如果迭代计数器达到 15，则跳转到 FINAL_CORRECTION 状态；否则保持 ITERATE 状态。

**状态 18：FINAL_CORRECTION**

FINAL_CORRECTION 状态是除法器的最终修正阶段，主要完成以下操作：

1. 计算最终商：Q = Q+ - Q-。

2. 计算最终余数：余数 = 部分余数的高 32 位。

3. 修正负余数：如果最终余数为负，需要修正商和余数。商减 1，余数加上除数。

4. 修正大余数：如果修正后的余数仍然大于等于除数，需要进一步修正。商加 1，余数减去除数。

5. 应用符号修正：根据商和余数的符号进行符号修正。如果商应该为负数，则对商取补码；如果余数应该为负数，则对余数取补码。

6. 选择结果：根据除法指令类型选择商或余数作为最终结果。

7. 设置结果有效标志：设置除法结果有效标志为 1。

8. 跳转到 DONE 状态。

**状态 19：DONE**

DONE 状态是除法器的完成阶段，主要完成以下操作：

1. 返回 IDLE 状态：将状态设置为 0，准备接受新的除法指令。

2. 重置结果有效标志：在下一个周期重置结果有效标志为 0。

### 8.4 商数字选择和余数更新

商数字选择和余数更新是 SRT 算法的核心操作，决定了除法的正确性和效率。

**商数字选择：**

商数字的选择基于部分余数的高位和除数的比较。具体的选择规则如下：

- q = +2 if P_high >= 2*D
- q = +1 if P_high >= D and P_high < 2*D
- q = 0 if P_high >= 0 and P_high < D (or P_high >= -D and P_high < 0)
- q = -1 if P_high >= -2*D and P_high < -D
- q = -2 if P_high < -2*D

其中 P_high 是部分余数的高 32 位，D 是除数。

商数字选择的实现方式是：首先计算比较边界（2*D、D、0、-D、-2*D），然后根据部分余数的高位选择对应的商数字。

**余数更新：**

余数的更新根据递归关系：P_{i+1} = 4 * P_i - q_i * D。具体实现步骤如下：

1. 左移部分余数：将部分余数左移 2 位（乘以 4）。

2. 计算商数字乘以除数：根据商数字计算 q * D。对于 q = +2，q * D = 2*D；对于 q = +1，q * D = D；对于 q = 0，q * D = 0；对于 q = -1，q * D = -D；对于 q = -2，q * D = -2*D。

3. 更新部分余数：部分余数 = 左移后的余数 - q * D。

余数更新的实现方式是：首先将部分余数左移 2 位，然后根据商数字选择对应的 q * D，最后从左移后的余数中减去 q * D。

**商累积器更新：**

商累积器的更新根据商数字的正负进行。具体更新规则如下：

- 如果 q >= 0：Q+ = Q+ * 4 + q，Q- = Q- * 4
- 如果 q < 0：Q+ = Q+ * 4，Q- = Q- * 4 + |q|

商累积器更新的实现方式是：首先将 Q+ 和 Q- 左移 2 位（乘以 4），然后根据商数字的正负选择更新 Q+ 或 Q-。如果 q >= 0，则将 q 加到 Q+；如果 q < 0，则将 |q| 加到 Q-。

**最终商计算：**

最终商的计算为：Q = Q+ - Q-。这个计算在 FINAL_CORRECTION 状态执行，得到最终的商值。

**最终余数计算：**

最终余数的计算为：余数 = 部分余数的高 32 位。这个计算在 FINAL_CORRECTION 状态执行，得到最终的余数值。由于部分余数可能为负数，需要进行符号修正。

---

## 9. jal/jalr指令实现

### 9.1 JAL指令实现（无条件跳转）

JAL（Jump and Link）指令是一条无条件跳转指令，它将程序计数器跳转到目标地址，并将返回地址（当前 PC 加 4）保存到目标寄存器中。JAL 指令常用于函数调用，返回地址用于函数返回。

**JAL 指令格式：**

JAL 指令使用 J 型指令格式，包含以下字段：

- opcode：操作码，值为 1101111
- rd：目标寄存器，用于保存返回地址
- imm：立即数，21 位有符号数，左移 1 位后作为跳转偏移量

**目标地址计算：**

JAL 指令的目标地址计算公式为：target_pc = pc + imm，其中 imm 是 21 位有符号立即数，左移 1 位后作为跳转偏移量。左移 1 位是因为跳转目标地址是 2 字节对齐的。

**返回地址计算：**

JAL 指令的返回地址计算公式为：return_addr = pc + 4，其中 pc 是当前指令的 PC 值。返回地址用于函数返回，通常保存到 ra 寄存器（x1）中。

**ID 阶段处理：**

在 ID 阶段，JAL 指令被识别为 J 型指令。ID 阶段生成以下控制信号：

- jump_op：设置为 1，表示当前指令是 JAL 指令
- reg_write：设置为 1（如果 rd 不为 0），表示需要写回寄存器
- alu_src：设置为 1，表示 ALU 的第二个输入是立即数
- immediate：设置为 J 型立即数，左移 1 位后符号扩展到 32 位

**EX 阶段处理：**

在 EX 阶段，JAL 指令执行以下操作：

1. 计算目标地址：target_pc = pc + imm

2. 计算返回地址：return_addr = pc + 4

3. 设置 ALU 输入：ALU 输入 A 设置为 pc，ALU 输入 B 设置为 4（用于计算返回地址）

4. 执行 ALU 操作：ALU 执行加法操作，计算返回地址

5. 生成控制信号：设置 PC 变化标志为 1，表示需要更新 PC

6. 生成预测结果：设置实际跳转标志为 1，实际目标地址为 target_pc

**PC 更新：**

当 JAL 指令执行完成时，PC 被更新为目标地址。PC 更新由 HazardUnit 控制，当检测到跳转指令时，刷新流水线并从目标地址重新开始执行。

**流水线刷新：**

JAL 指令会导致流水线刷新，清除 IF 和 ID 阶段的指令。刷新的原因是 JAL 指令改变了 PC，IF 阶段获取的下一条指令不再有效，需要从新的 PC 地址重新获取指令。

### 9.2 JALR指令实现（寄存器跳转）

JALR（Jump and Link Register）指令是一条寄存器跳转指令，它将程序计数器跳转到寄存器值加上立即数的目标地址，并将返回地址（当前 PC 加 4）保存到目标寄存器中。JALR 指令常用于函数返回，通过将返回地址保存到 ra 寄存器，然后使用 JALR 指令跳转回调用者。

**JALR 指令格式：**

JALR 指令使用 I 型指令格式，包含以下字段：

- opcode：操作码，值为 1100111
- rd：目标寄存器，用于保存返回地址
- rs1：源寄存器，包含基础地址
- imm：立即数，12 位有符号数，作为跳转偏移量

**目标地址计算：**

JALR 指令的目标地址计算公式为：target_pc = (rs1 + imm) & ~1，其中 rs1 是源寄存器的值，imm 是 12 位有符号立即数，符号扩展到 32 位后作为跳转偏移量。与 ~1 进行与操作是为了清除最低位，保证目标地址是 2 字节对齐的。

**返回地址计算：**

JALR 指令的返回地址计算公式为：return_addr = pc + 4，其中 pc 是当前指令的 PC 值。返回地址用于函数返回，通常保存到 ra 寄存器（x1）中。

**ID 阶段处理：**

在 ID 阶段，JALR 指令被识别为 JR 型指令。ID 阶段生成以下控制信号：

- jumpr_op：设置为 1，表示当前指令是 JALR 指令
- reg_write：设置为 1（如果 rd 不为 0），表示需要写回寄存器
- alu_src：设置为 1，表示 ALU 的第二个输入是立即数
- immediate：设置为 I 型立即数，符号扩展到 32 位

**EX 阶段处理：**

在 EX 阶段，JALR 指令执行以下操作：

1. 计算目标地址：target_pc = (rs1 + imm) & ~1

2. 计算返回地址：return_addr = pc + 4

3. 设置 ALU 输入：ALU 输入 A 设置为 pc，ALU 输入 B 设置为 4（用于计算返回地址）

4. 执行 ALU 操作：ALU 执行加法操作，计算返回地址

5. 生成控制信号：设置 PC 变化标志为 1，表示需要更新 PC

6. 生成预测结果：设置实际跳转标志为 1，实际目标地址为 target_pc

**PC 更新：**

当 JALR 指令执行完成时，PC 被更新为目标地址。PC 更新由 HazardUnit 控制，当检测到寄存器跳转指令时，刷新流水线并从目标地址重新开始执行。

**流水线刷新：**

JALR 指令会导致流水线刷新，清除 IF 和 ID 阶段的指令。刷新的原因是 JALR 指令改变了 PC，IF 阶段获取的下一条指令不再有效，需要从新的 PC 地址重新获取指令。

### 9.3 返回地址计算

返回地址计算是 JAL 和 JALR 指令的关键功能，它将当前 PC 加 4 的值保存到目标寄存器中，用于函数返回。

**返回地址计算公式：**

返回地址计算公式为：return_addr = pc + 4，其中 pc 是当前指令的 PC 值。由于 RISC-V 指令是 4 字节对齐的，下一条指令的地址总是当前 PC 加 4。

**返回地址保存：**

返回地址被保存到目标寄存器中。目标寄存器由 rd 字段指定，可以是任何通用寄存器（包括 x0，但 x0 是硬连线为 0 的，不会被写入）。

**返回地址使用：**

返回地址通常用于函数返回。函数调用时，使用 JAL 指令跳转到函数入口，并将返回地址保存到 ra 寄存器（x1）。函数返回时，使用 JALR 指令从 ra 寄存器跳转回调用者。

### 9.4 PC更新机制

PC 更新机制是 JAL 和 JALR 指令执行的关键，它决定了程序控制流的转移。

**PC 更新触发条件：**

PC 更新的触发条件包括：JAL 指令、JALR 指令和分支预测错误。当这些条件满足时，HazardUnit 生成刷新信号，触发 PC 更新。

**PC 更新逻辑：**

PC 更新逻辑由 HazardUnit 控制，根据触发条件选择正确的 PC 值。对于 JAL 指令，PC 被更新为 target_pc（pc + imm）；对于 JALR 指令，PC 被更新为 target_pc（(rs1 + imm) & ~1）；对于分支预测错误，PC 被更新为 correct_pc（根据实际跳转标志计算的正确 PC）。

**PC 更新时机：**

PC 更新在 HazardUnit 中执行，在时钟边沿更新 PC 寄存器。PC 更新后，IF 阶段从新的 PC 地址获取指令。

**流水线刷新：**

PC 更新后，需要刷新流水线，清除 IF 和 ID 阶段的指令。刷新的原因是 PC 改变后，IF 阶段获取的下一条指令不再有效，需要从新的 PC 地址重新获取指令。

流水线刷新包括以下操作：

1. 清除 IF/ID 寄存器：将 if_id_pc、if_id_instruction 和 if_id_prediction_info 设置为 0

2. 清除 ID/EX 寄存器：将 id_ex_control、id_ex_immediate、id_ex_rs1_idx、id_ex_rs2_idx 和 id_ex_prediction_info 设置为 0

3. 设置 if_id_valid 为 0：表示 IF/ID 寄存器中的指令无效

4. 设置 id_ex_valid 为 0：表示 ID/EX 寄存器中的指令无效

**预测结果更新：**

对于分支指令，PC 更新后还需要更新 BTB 和 BHT。BTB 更新包括设置目标地址和有效标志；BHT 更新包括根据实际跳转方向更新 2 位饱和计数器。

对于 JAL 和 JALR 指令，不需要更新 BTB 和 BHT，因为它们不是分支指令。

---

## 10. lb/lh/sb/sh指令实现

### 10.1 Load指令（LB、LH、LW、LBU、LHU）

Load 指令从内存中读取数据，并根据加载类型进行符号扩展或零扩展，然后将结果写回目标寄存器。Load 指令包括 LB（加载有符号字节）、LH（加载有符号半字）、LW（加载字）、LBU（加载无符号字节）、LHU（加载无符号半字）。

**Load 指令格式：**

Load 指令使用 I 型指令格式，包含以下字段：

- opcode：操作码，值为 0000011
- rd：目标寄存器，用于保存加载数据
- rs1：源寄存器，包含基础地址
- funct3：功能码，指定加载类型（000=LB，001=LH，010=LW，100=LBU，101=LHU）
- imm：立即数，12 位有符号数，作为地址偏移量

**地址计算：**

Load 指令的地址计算公式为：addr = rs1 + imm，其中 rs1 是源寄存器的值，imm 是 12 位有符号立即数，符号扩展到 32 位后作为地址偏移量。

**ID 阶段处理：**

在 ID 阶段，Load 指令被识别为 L 型指令。ID 阶段生成以下控制信号：

- mem_read：设置为 1，表示需要从内存读取数据
- reg_write：设置为 1（如果 rd 不为 0），表示需要写回寄存器
- mem_to_reg：设置为 1，表示写回数据来自内存
- load_type：设置为 funct3，指定加载类型
- alu_src：设置为 1，表示 ALU 的第二个输入是立即数
- immediate：设置为 I 型立即数，符号扩展到 32 位

**EX 阶段处理：**

在 EX 阶段，Load 指令执行以下操作：

1. 计算地址：addr = rs1 + imm

2. 执行 ALU 操作：ALU 执行加法操作，计算地址

3. 生成控制信号：保持 mem_read、reg_write、mem_to_reg 和 load_type 信号

4. 传递数据：将 rs2 数据（未使用）传递给 MEM 阶段

**MEM 阶段处理：**

在 MEM 阶段，Load 指令执行以下操作：

1. 计算字地址：word_addr = addr >> 2

2. 计算字节偏移：byte_offset = addr[0:1]

3. 读取内存数据：从 SRAM 读取一个字（32 位）

4. 传递数据：将读取的原始数据和地址传递给 WB 阶段

**WB 阶段处理：**

在 WB 阶段，Load 指令执行以下操作：

1. 根据加载类型处理数据

2. 对于 LB：根据字节偏移选择对应的字节，然后进行符号扩展到 32 位

3. 对于 LH：根据字节偏移选择对应的半字，然后进行符号扩展到 32 位

4. 对于 LW：直接使用 32 位数据

5. 对于 LBU：根据字节偏移选择对应的字节，然后进行零扩展到 32 位

6. 对于 LHU：根据字节偏移选择对应的半字，然后进行零扩展到 32 位

7. 写回寄存器：将处理后的数据写入目标寄存器

**字节选择：**

字节选择根据地址的低 2 位进行。地址的低 2 位表示字节偏移，0 表示最低字节，1 表示次低字节，2 表示次高字节，3 表示最高字节。

**半字选择：**

半字选择根据地址的低 2 位的最高位进行。地址的低 2 位的最高位表示半字偏移，0 表示低半字，1 表示高半字。

**符号扩展：**

符号扩展时，使用选中字节的最高位作为符号位，高 24 位用符号位填充；对于半字，使用选中半字的最高位作为符号位，高 16 位用符号位填充。

**零扩展：**

零扩展时，高 24 位（字节）或高 16 位（半字）填充 0。

### 10.2 Store指令（SB、SH、SW）

Store 指令将数据写入内存，根据存储类型写入字节、半字或字。Store 指令包括 SB（存储字节）、SH（存储半字）、SW（存储字）。

**Store 指令格式：**

Store 指令使用 S 型指令格式，包含以下字段：

- opcode：操作码，值为 0100011
- rs1：源寄存器 1，包含基础地址
- rs2：源寄存器 2，包含要存储的数据
- funct3：功能码，指定存储类型（000=SB，001=SH，010=SW）
- imm：立即数，12 位有符号数，作为地址偏移量

**地址计算：**

Store 指令的地址计算公式为：addr = rs1 + imm，其中 rs1 是源寄存器 1 的值，imm 是 12 位有符号立即数，符号扩展到 32 位后作为地址偏移量。

**ID 阶段处理：**

在 ID 阶段，Store 指令被识别为 S 型指令。ID 阶段生成以下控制信号：

- mem_write：设置为 1，表示需要向内存写入数据
- store_type：设置为 funct3 的低 2 位，指定存储类型（00=SB，01=SH，10=SW）
- alu_src：设置为 1，表示 ALU 的第二个输入是立即数
- immediate：设置为 S 型立即数，符号扩展到 32 位

**EX 阶段处理：**

在 EX 阶段，Store 指令执行以下操作：

1. 计算地址：addr = rs1 + imm

2. 执行 ALU 操作：ALU 执行加法操作，计算地址

3. 生成控制信号：保持 mem_write 和 store_type 信号

4. 传递数据：将 rs2 数据传递给 MEM 阶段

**MEM 阶段处理：**

在 MEM 阶段，Store 指令根据存储类型执行不同的操作。

对于 SW 指令，执行以下操作：

1. 计算字地址：word_addr = addr >> 2

2. 写入内存数据：将 rs2 数据直接写入 SRAM

对于 SB 和 SH 指令，执行读-修改-写操作，详见下一节。

### 10.3 读-修改-写（RMW）机制

读-修改-写（Read-Modify-Write，RMW）机制用于处理字节存储（SB）和半字存储（SH）指令。由于 SRAM 只支持字级别的访问，SB 和 SH 指令需要先读取整个字，修改对应的字节或半字，然后写回整个字。

**RMW 状态机：**

RMW 操作使用状态机控制，状态机有三种状态：IDLE（空闲）、READ（读取）和 WRITE（写入）。

**IDLE 状态：**

在 IDLE 状态，状态机等待 SB 或 SH 指令。当检测到 SB 或 SH 指令时，保存当前指令的地址、数据和存储类型，然后进入 WRITE 状态。

**READ 状态：**

在 READ 状态，状态机从 SRAM 读取整个字。读取的数据用于后续的修改操作。在本实现中，READ 状态未使用，直接进入 WRITE 状态。

**WRITE 状态：**

在 WRITE 状态，状态机执行修改和写回操作。修改操作根据存储类型进行，然后将修改后的字写回 SRAM。写回完成后，状态机返回 IDLE 状态。

**SB 指令的 RMW 操作：**

对于 SB 指令，RMW 操作包括以下步骤：

1. 读取整个字：从 SRAM 读取一个字（32 位）

2. 选择要修改的字节：根据字节偏移选择对应的字节

3. 构建写入数据：将数据字节放在正确的位置，其他位置填充 0

4. 构建掩码：将对应字节的位置设为 0，其他位置设为 1

5. 合并数据：原始数据与掩码相与，然后与写入数据相或

6. 写回 SRAM：将合并后的字写回 SRAM

**字节偏移处理：**

字节偏移根据地址的低 2 位确定。地址的低 2 位表示字节偏移，0 表示最低字节，1 表示次低字节，2 表示次高字节，3 表示最高字节。

**SB 写入数据构建：**

SB 写入数据根据字节偏移将数据字节放在正确的位置。如果字节偏移为 0，数据字节放在最低 8 位；如果字节偏移为 1，数据字节放在次低 8 位；如果字节偏移为 2，数据字节放在次高 8 位；如果字节偏移为 3，数据字节放在最高 8 位。其他位置填充 0。

**SB 掩码构建：**

SB 掩码根据字节偏移将对应字节的位置设为 0，其他位置设为 1。如果字节偏移为 0，掩码为 0xFFFFFF00；如果字节偏移为 1，掩码为 0xFFFF00FF；如果字节偏移为 2，掩码为 0xFF00FFFF；如果字节偏移为 3，掩码为 0x00FFFFFF。

**SH 指令的 RMW 操作：**

对于 SH 指令，RMW 操作包括以下步骤：

1. 读取整个字：从 SRAM 读取一个字（32 位）

2. 选择要修改的半字：根据字节偏移选择对应的半字

3. 构建写入数据：将数据半字放在正确的位置，其他位置填充 0

4. 构建掩码：将对应半字的位置设为 0，其他位置设为 1

5. 合并数据：原始数据与掩码相与，然后与写入数据相或

6. 写回 SRAM：将合并后的字写回 SRAM

**半字偏移处理：**

半字偏移根据地址的低 2 位的最高位确定。地址的低 2 位的最高位表示半字偏移，0 表示低半字，1 表示高半字。

**SH 写入数据构建：**

SH 写入数据根据半字偏移将数据半字放在正确的位置。如果半字偏移为 0，数据半字放在低 16 位；如果半字偏移为 1，数据半字放在高 16 位。其他位置填充 0。

**SH 掩码构建：**

SH 掩码根据半字偏移将对应半字的位置设为 0，其他位置设为 1。如果半字偏移为 0，掩码为 0xFFFF0000；如果半字偏移为 1，掩码为 0x0000FFFF。

**RMW 流水线控制：**

RMW 操作需要两个周期完成，期间流水线需要暂停。在第一个周期，状态机从 IDLE 转换到 WRITE，保存地址、数据和存储类型。在第二个周期，状态机执行修改和写回操作，然后返回 IDLE 状态。

在 RMW 操作期间，MEM/WB 寄存器被清零，不写回寄存器。这是因为 SB 和 SH 指令不需要写回寄存器，只是修改内存数据。

**SB/SH 暂停信号：**

SB/SH 暂停信号用于指示当前是否正在进行 SB/SH 操作。暂停信号在 IDLE 状态检测到 SB 或 SH 指令时为 1，在 WRITE 状态时也为 1。暂停信号被传递给 HazardUnit，用于生成暂停信号，暂停 IF、ID 和 EX 阶段。

### 10.4 字节和半字对齐处理

字节和半字对齐处理是 Load 和 Store 指令的关键，它决定了如何从字地址中选择对应的字节或半字。

**字节对齐：**

字节对齐是指字节地址的低 2 位表示字节在字中的位置。地址的低 2 位为 0 表示字节在字的最低 8 位，为 1 表示字节在字的次低 8 位，为 2 表示字节在字的次高 8 位，为 3 表示字节在字的最高 8 位。

**半字对齐：**

半字对齐是指半字地址的低 2 位的最高位表示半字在字中的位置。地址的低 2 位的最高位为 0 表示半字在字的低 16 位，为 1 表示半字在字的高 16 位。

**Load 指令的对齐处理：**

对于 Load 指令，对齐处理用于从读取的字中选择对应的字节或半字。选择方式根据加载类型确定：

- 对于 LB：根据字节偏移选择对应的字节
- 对于 LH：根据字节偏移选择对应的半字
- 对于 LW：直接使用整个字
- 对于 LBU：根据字节偏移选择对应的字节
- 对于 LHU：根据字节偏移选择对应的半字

**Store 指令的对齐处理：**

对于 Store 指令，对齐处理用于将数据字节或半字放在正确的位置。放置方式根据存储类型确定：

- 对于 SB：根据字节偏移将数据字节放在正确的位置
- 对于 SH：根据字节偏移将数据半字放在正确的位置
- 对于 SW：直接使用整个字

**对齐处理实现：**

对齐处理的实现方式是使用多路选择器。对于字节选择，根据字节偏移从 4 个字节中选择一个；对于半字选择，根据字节偏移的最高位从 2 个半字中选择一个。

对于写入数据构建，使用拼接操作将数据字节或半字放在正确的位置，其他位置填充 0。对于掩码构建，使用常量或拼接操作将对应位置设为 0，其他位置设为 1。

---

## 附录

### A. 指令集参考

#### RV32I 基础整数指令集

**算术运算指令：**

- ADD：有符号加法，rd = rs1 + rs2
- SUB：有符号减法，rd = rs1 - rs2

**逻辑运算指令：**

- AND：按位与，rd = rs1 & rs2
- OR：按位或，rd = rs1 | rs2
- XOR：按位异或，rd = rs1 ^ rs2

**移位指令：**

- SLL：逻辑左移，rd = rs1 << rs2[4:0]
- SRL：逻辑右移，rd = rs1 >> rs2[4:0]
- SRA：算术右移，rd = rs1 >>> rs2[4:0]

**比较指令：**

- SLT：有符号小于，rd = (rs1 < rs2) ? 1 : 0
- SLTU：无符号小于，rd = (rs1 < rs2) ? 1 : 0

**立即数指令：**

- ADDI：加立即数，rd = rs1 + imm
- ANDI：与立即数，rd = rs1 & imm
- ORI：或立即数，rd = rs1 | imm
- XORI：异或立即数，rd = rs1 ^ imm

**加载指令：**

- LB：加载有符号字节，rd = sign_extend(mem[rs1 + imm][7:0])
- LH：加载有符号半字，rd = sign_extend(mem[rs1 + imm][15:0])
- LW：加载字，rd = mem[rs1 + imm]
- LBU：加载无符号字节，rd = zero_extend(mem[rs1 + imm][7:0])
- LHU：加载无符号半字，rd = zero_extend(mem[rs1 + imm][15:0])

**存储指令：**

- SB：存储字节，mem[rs1 + imm][7:0] = rs2[7:0]
- SH：存储半字，mem[rs1 + imm][15:0] = rs2[15:0]
- SW：存储字，mem[rs1 + imm] = rs2

**分支指令：**

- BEQ：等于跳转，if (rs1 == rs2) pc = pc + imm
- BNE：不等于跳转，if (rs1 != rs2) pc = pc + imm
- BLT：小于跳转，if (rs1 < rs2) pc = pc + imm
- BGE：大于等于跳转，if (rs1 >= rs2) pc = pc + imm
- BLTU：无符号小于跳转，if (rs1 < rs2) pc = pc + imm
- BGEU：无符号大于等于跳转，if (rs1 >= rs2) pc = pc + imm

**跳转指令：**

- JAL：无条件跳转并链接，rd = pc + 4, pc = pc + imm
- JALR：寄存器跳转并链接，rd = pc + 4, pc = (rs1 + imm) & ~1

**立即数指令：**

- LUI：加载高位立即数，rd = imm << 12
- AUIPC：添加高位立即数到 PC，rd = pc + (imm << 12)

#### RV32M 乘除法扩展指令集

**乘法指令：**

- MUL：有符号乘法，rd = (rs1 * rs2)[31:0]
- MULH：有符号乘法高位，rd = (rs1 * rs2)[63:32]
- MULHSU：有符号乘无符号高位，rd = (rs1 * rs2)[63:32]
- MULHU：无符号乘法高位，rd = (rs1 * rs2)[63:32]

**除法指令：**

- DIV：有符号除法，rd = rs1 / rs2
- DIVU：无符号除法，rd = rs1 / rs2

**取余指令：**

- REM：有符号取余，rd = rs1 % rs2
- REMU：无符号取余，rd = rs1 % rs2

### B. 信号定义表

#### 控制信号定义

**48 位控制信号格式：**

- [47:45] - div_op（3 位除法操作码）
- [44:42] - mul_op（3 位乘法操作码）
- [41:30] - 立即数低 12 位
- [29:25] - rd 地址（5 位）
- [24] - alu_a_zero（1 位，LUI 需要 alu_a=0）
- [23:22] - 存储类型（2 位，00=SB，01=SH，10=SW）
- [21] - jumpr_op（1 位，寄存器跳转标志）
- [20] - jump_op（1 位，跳转指令标志）
- [19:17] - branch_op（3 位，分支操作类型）
- [16:14] - load_type（3 位，000=LB，001=LH，010=LW，100=LBU，101=LHU）
- [13:11] - 保留位（3 位）
- [10:9] - alu_src（2 位，ALU 输入选择）
- [8] - mem_to_reg（1 位，内存到寄存器）
- [7] - reg_write（1 位，寄存器写使能）
- [6] - mem_write（1 位，内存写使能）
- [5] - mem_read（1 位，内存读使能）
- [4:0] - alu_op（5 位，ALU 操作码）

#### ALU 操作码定义

- 00000 - ADD
- 00001 - SUB
- 00010 - SLL
- 00011 - SLT
- 00100 - XOR
- 00101 - SRL
- 00110 - SRA
- 00111 - SLTU
- 01000 - OR
- 01001 - AND

#### 分支操作码定义

- 001 - BEQ
- 010 - BNE
- 011 - BLT
- 100 - BGE
- 101 - BLTU
- 110 - BGEU

#### 乘法操作码定义

- 000 - 非乘法指令
- 001 - MUL（有符号乘法，返回低 32 位）
- 010 - MULH（有符号乘法，返回高 32 位）
- 011 - MULHSU（有符号乘无符号，返回高 32 位）
- 100 - MULHU（无符号乘法，返回高 32 位）

#### 除法操作码定义

- 000 - 非除法指令
- 001 - DIV（有符号除法）
- 010 - DIVU（无符号除法）
- 011 - REM（有符号取余）
- 100 - REMU（无符号取余）

#### 预测信息定义

**34 位预测信息格式：**

- [33:2] - predicted_pc（32 位预测的 PC）
- [1] - predict_taken（1 位预测是否跳转）
- [0] - btb_hit（1 位 BTB 是否命中）

#### 预测结果定义

**103 位预测结果格式：**

- [102] - is_jumpr（1 位是否为 JALR 指令）
- [101] - is_jump（1 位是否为 JAL 指令）
- [100] - is_branch（1 位是否为分支指令）
- [99:68] - pc_in（32 位分支指令的 PC）
- [67] - predict_taken（1 位预测是否跳转）
- [66] - btb_hit（1 位 BTB 是否命中）
- [65:34] - actual_target_pc（32 位实际目标地址）
- [33] - actual_taken（1 位实际跳转标志）
- [32:1] - correct_pc（32 位正确的 PC）
- [0] - mispredict（1 位预测错误标志）

### C. 时序图

#### 正常指令执行时序

```
周期 1：IF 阶段获取指令 N
周期 2：IF 阶段获取指令 N+1，ID 阶段解码指令 N
周期 3：IF 阶段获取指令 N+2，ID 阶段解码指令 N+1，EX 阶段执行指令 N
周期 4：IF 阶段获取指令 N+3，ID 阶段解码指令 N+2，EX 阶段执行指令 N+1，MEM 阶段访问内存指令 N
周期 5：IF 阶段获取指令 N+4，ID 阶段解码指令 N+3，EX 阶段执行指令 N+2，MEM 阶段访问内存指令 N+1，WB 阶段写回指令 N
```

#### Load-Use 冒险时序

```
周期 1：IF 阶段获取 Load 指令
周期 2：IF 阶段获取 Use 指令，ID 阶段解码 Load 指令
周期 3：IF 阶段暂停，ID 阶段暂停，EX 阶段执行 Load 指令
周期 4：IF 阶段获取 Use 指令，ID 阶段解码 Use 指令，EX 阶段执行 Use 指令（使用前递数据），MEM 阶段访问内存 Load 指令
周期 5：IF 阶段获取下一条指令，ID 阶段解码 Use 指令，EX 阶段执行下一条指令，MEM 阶段访问内存 Use 指令，WB 阶段写回 Load 指令
```

#### 乘法指令时序

```
周期 1：IF 阶段获取 MUL 指令，ID 阶段解码 MUL 指令
周期 2：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行 MUL 指令（周期 1：符号转换和部分积生成）
周期 3：IF 阶段暂停，ID 阶段暂停，EX 阶段执行 MUL 指令（周期 2：继续 CSA 压缩）
周期 4：IF 阶段暂停，ID 阶段暂停，EX 阶段执行 MUL 指令（周期 3：最终加法和结果选择）
周期 5：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行下一条指令，MEM 阶段访问内存 MUL 指令
周期 6：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行下一条指令，MEM 阶段访问内存下一条指令，WB 阶段写回 MUL 指令
```

#### 除法指令时序

```
周期 1：IF 阶段获取 DIV 指令，ID 阶段解码 DIV 指令
周期 2：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行 DIV 指令（周期 1：初始化）
周期 3-18：IF 阶段暂停，ID 阶段暂停，EX 阶段执行 DIV 指令（周期 2-17：迭代）
周期 19：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行 DIV 指令（周期 18：最终修正）
周期 20：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行下一条指令，MEM 阶段访问内存 DIV 指令
周期 21：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行下一条指令，MEM 阶段访问内存下一条指令，WB 阶段写回 DIV 指令
```

#### 分支预测错误时序

```
周期 1：IF 阶段获取分支指令（预测跳转）
周期 2：IF 阶段获取目标指令，ID 阶段解码分支指令
周期 3：IF 阶段获取下一条指令，ID 阶段解码目标指令，EX 阶段执行分支指令（预测错误）
周期 4：IF 阶段获取正确指令，ID 阶段解码正确指令，EX 阶段执行正确指令，MEM 阶段访问内存目标指令
周期 5：IF 阶段获取下一条指令，ID 阶段解码下一条指令，EX 阶段执行下一条指令，MEM 阶段访问内存正确指令，WB 阶段写回目标指令
```

### D. 参考资料

#### RISC-V 规范

- RISC-V 基础整数指令集规范：https://riscv.org/technical/specifications/
- RISC-V 乘除法扩展规范：https://riscv.org/technical/specifications/

#### Assassyn 语言文档

- Assassyn 语言手册：/home/zhangboju/assassyn/docs/language.md
- Assassyn 开发者指南：/home/zhangboju/assassyn/docs/developer/
- Assassyn 示例代码：/home/zhangboju/assassyn/examples/

#### CPU 架构参考

- 五级流水线设计：/home/zhangboju/assassyn/examples/minor-cpu/
- 分支预测：/home/zhangboju/assassyn/examples/minor-cpu/src/br_pre_main.py
- 乘法器实现：/home/zhangboju/assassyn/python/assassyn/ip/multiply.py

#### 学术参考

- Patterson, D. A., & Hennessy, J. L. (2017). Computer Organization and Design RISC-V Edition. Morgan Kaufmann.
- Hennessy, J. L., & Patterson, D. A. (2012). Computer Architecture: A Quantitative Approach. Morgan Kaufmann.
- Harris, D. M., & Harris, S. L. (2015). Digital Design and Computer Architecture. Morgan Kaufmann.
